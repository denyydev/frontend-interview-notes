# React Props

Передача данных в компоненты и работа с props в React.  
Материал представлен в формате **вопрос–ответ**.

1. [Что такое props](#1-что-такое-props)
2. [Являются ли props мутабельными](#2-являются-ли-props-мутабельными)
3. [Как передать данные в компонент](#3-как-передать-данные-в-компонент)
4. [Как передать функцию в props](#4-как-передать-функцию-в-props)
5. [Что такое children](#5-что-такое-children)
6. [Как типично используют children](#6-как-типично-используют-children)
7. [Что будет, если изменить props напрямую](#7-что-будет-если-изменить-props-напрямую)

## 1. Что такое props

Props (properties) — это **данные, которые передаются от родительского компонента к дочернему**. Props позволяют компонентам быть переиспользуемыми и настраиваемыми.

### Простое определение

Props — это **аргументы функции**, которые React передаёт компоненту.

```jsx
function Greeting(props) {
  return <h1>Привет, {props.name}!</h1>;
}

// Использование
<Greeting name="Алекс" />  // "Привет, Алекс!"
<Greeting name="Мария" />  // "Привет, Мария!"
```

Здесь `name` — это prop, переданный компоненту `Greeting`.

### Деструктуризация props

Обычно props деструктурируют для удобства:

```jsx
// Без деструктуризации
function Greeting(props) {
  return <h1>Привет, {props.name}!</h1>;
}

// С деструктуризацией (рекомендуется)
function Greeting({ name }) {
  return <h1>Привет, {name}!</h1>;
}
```

Второй вариант **читабельнее и короче**.

### Передача нескольких props

Можно передавать **сколько угодно props**:

```jsx
function UserCard({ name, email, age }) {
  return (
    <div>
      <h2>{name}</h2>
      <p>Email: {email}</p>
      <p>Возраст: {age}</p>
    </div>
  );
}

// Использование
<UserCard name="Алекс" email="alex@example.com" age={25} />;
```

### Типы данных в props

В props можно передавать **любые типы данных**:

```jsx
function Component({
  name, // строка
  age, // число
  isActive, // булево значение
  items, // массив
  user, // объект
  onClick, // функция
}) {
  return <div>...</div>;
}

// Использование
<Component
  name="Алекс"
  age={25}
  isActive={true}
  items={[1, 2, 3]}
  user={{ name: "Алекс", age: 25 }}
  onClick={() => console.log("Клик")}
/>;
```

**Важно:** для строк можно передавать без фигурных скобок, для остальных — с `{}`.

### Props только для чтения

**Props неизменяемы (immutable)** — компонент не должен изменять props, которые он получает:

```jsx
function BadComponent({ name }) {
  name = "Другое имя"; // ❌ Нельзя!
  return <h1>{name}</h1>;
}
```

React следует принципу: **данные текут сверху вниз** (от родителя к ребенку).

### Передача props вниз

Props можно передавать через **несколько уровней компонентов**:

```jsx
function App() {
  const user = { name: "Алекс", email: "alex@example.com" };
  return <Layout user={user} />;
}

function Layout({ user }) {
  return <Header user={user} />;
}

function Header({ user }) {
  return <UserProfile user={user} />;
}

function UserProfile({ user }) {
  return <div>{user.name}</div>;
}
```

Данные передаются от `App` → `Layout` → `Header` → `UserProfile`.

### Значения по умолчанию

Можно задать **значения по умолчанию** для props:

```jsx
function Greeting({ name = "Гость" }) {
  return <h1>Привет, {name}!</h1>;
}

<Greeting />           // "Привет, Гость!"
<Greeting name="Алекс" />  // "Привет, Алекс!"
```

Или использовать дефолтные props:

```jsx
function Greeting({ name }) {
  return <h1>Привет, {name}!</h1>;
}

Greeting.defaultProps = {
  name: "Гость",
};
```

### Визуальная аналогия

Props — как **параметры функции**:

- Функция `calculate(a, b)` принимает `a` и `b`
- Компонент `<Button text="Сохранить" />` принимает `text`

Компонент — это функция, props — её аргументы.

### Итоги

- Props — данные, передаваемые от родителя к ребенку
- Props делают компоненты переиспользуемыми
- Props неизменяемы (нельзя изменять внутри компонента)
- Можно передавать любые типы данных
- Рекомендуется использовать деструктуризацию
- Данные текут сверху вниз (от родителя к ребенку)

---

## 2. Являются ли props мутабельными

**Нет, props неизменяемы (immutable)**. Компонент не должен изменять props, которые он получает.

### Props только для чтения

React следует принципу **"props — только для чтения"**:

```jsx
function BadComponent({ name }) {
  name = "Другое имя"; // ❌ Неправильно!
  return <h1>{name}</h1>;
}
```

Это **не сработает**, и React выдаст предупреждение в строгом режиме.

### Почему props неизменяемы?

#### 1. Предсказуемость

Если компонент не может изменять props, его поведение **предсказуемо**:

```jsx
function Greeting({ name }) {
  // Мы точно знаем, что name не изменится внутри компонента
  return <h1>Привет, {name}!</h1>;
}

<Greeting name="Алекс" />; // Всегда покажет "Привет, Алекс!"
```

#### 2. Односторонний поток данных

Данные в React текут **только сверху вниз** (от родителя к ребенку):

```
Родитель (App) → Ребёнок (Greeting)
     ↓                ↓
   state          props
  (источник)    (только чтение)
```

Если нужно обновить данные, это делается в **родительском компоненте**.

#### 3. Отладка

Когда props неизменяемы, легче **отлаживать**:

- Можно быть уверенным, что props не изменились внутри компонента
- Легче отследить, откуда пришли данные

### Что происходит при попытке изменения?

```jsx
function Component({ items }) {
  items.push("Новый элемент"); // ❌ Неправильно!
  return <div>{items.length}</div>;
}
```

**В строгом режиме** React выдаст предупреждение:

```
Cannot assign to read only property 'push' of object
```

### Правильный подход

Если нужно изменить данные, делай это в **родительском компоненте**:

```jsx
// Родительский компонент
function App() {
  const [items, setItems] = useState(["Элемент 1"]);

  const addItem = () => {
    setItems([...items, "Новый элемент"]);
  };

  return <Component items={items} onAddItem={addItem} />;
}

// Дочерний компонент
function Component({ items, onAddItem }) {
  return (
    <div>
      <ul>
        {items.map((item) => (
          <li key={item}>{item}</li>
        ))}
      </ul>
      <button onClick={onAddItem}>Добавить</button>
    </div>
  );
}
```

Данные **управляются родителем**, ребенок только **отображает** их и **вызывает функции** для обновления.

### Объекты и массивы в props

**Важно:** хотя сам props-объект неизменяем, **внутренности объектов и массивов** можно изменить (но это плохая практика):

```jsx
function Component({ user }) {
  user.name = "Другое имя"; // ⚠️ Технически возможно, но НЕПРАВИЛЬНО!
  return <div>{user.name}</div>;
}
```

Это **не вызовет ошибку**, но нарушит принципы React и может привести к багам.

### Правильная работа с объектами и массивами

Если нужно изменить данные, **создавай новые объекты/массивы**:

```jsx
// ❌ Неправильно
function Component({ items }) {
  items.push("Новый"); // Мутация
  return <div>{items.length}</div>;
}

// ✅ Правильно (в родителе)
function App() {
  const [items, setItems] = useState(["Элемент 1"]);

  const addItem = () => {
    setItems([...items, "Новый"]); // Новый массив
  };

  return <Component items={items} onAddItem={addItem} />;
}
```

### ⚠️ Частая ошибка

Попытка изменить props напрямую:

```jsx
function UserCard({ user }) {
  // ❌ Неправильно
  user.name = "Новое имя";
  return <div>{user.name}</div>;
}

// ✅ Правильно
function UserCard({ user, onUpdateUser }) {
  const handleUpdate = () => {
    onUpdateUser({ ...user, name: "Новое имя" });
  };
  return (
    <div>
      <div>{user.name}</div>
      <button onClick={handleUpdate}>Изменить</button>
    </div>
  );
}
```

### Итоги

- Props **неизменяемы** (immutable)
- Компонент не должен изменять props
- Данные управляются родительским компонентом
- Изменения делаются через функции, переданные в props
- Попытка изменить props может привести к предупреждениям и багам

---

## 3. Как передать данные в компонент

Данные в компонент передаются через **props** (properties). Это делается как передача атрибутов в HTML-теги.

### Базовый синтаксис

```jsx
// Компонент
function Greeting({ name }) {
  return <h1>Привет, {name}!</h1>;
}

// Передача данных
<Greeting name="Алекс" />;
```

Здесь `name="Алекс"` — это prop, который передаётся компоненту.

### Передача разных типов данных

#### Строки

```jsx
<UserCard name="Алекс" />
```

Строки можно передавать **без фигурных скобок**.

#### Числа

```jsx
<UserCard age={25} />
```

Числа передаются **в фигурных скобках**.

#### Булевы значения

```jsx
<Button isActive={true} />
<Button isActive={false} />
<Button isActive />  // true по умолчанию
```

Булевы значения передаются **в фигурных скобках**. Можно использовать сокращённый синтаксис.

#### Массивы

```jsx
<TodoList items={["Задача 1", "Задача 2"]} />;

// Или
const items = ["Задача 1", "Задача 2"];
<TodoList items={items} />;
```

#### Объекты

```jsx
<UserCard user={{ name: "Алекс", age: 25 }} />;

// Или
const user = { name: "Алекс", age: 25 };
<UserCard user={user} />;
```

#### Функции

```jsx
<Button onClick={() => console.log("Клик")} />;

// Или
const handleClick = () => console.log("Клик");
<Button onClick={handleClick} />;
```

### Передача нескольких props

```jsx
function UserCard({ name, email, age, isActive }) {
  return (
    <div>
      <h2>{name}</h2>
      <p>Email: {email}</p>
      <p>Возраст: {age}</p>
      {isActive && <p>Активен</p>}
    </div>
  );
}

// Использование
<UserCard name="Алекс" email="alex@example.com" age={25} isActive={true} />;
```

### Spread оператор

Если у тебя есть объект с props, можно использовать **spread оператор**:

```jsx
const userProps = {
  name: "Алекс",
  email: "alex@example.com",
  age: 25
};

<UserCard {...userProps} />

// Эквивалентно:
<UserCard
  name="Алекс"
  email="alex@example.com"
  age={25}
/>
```

Это удобно, когда нужно передать много props.

### Значения по умолчанию

Можно задать **значения по умолчанию** для props:

```jsx
function Greeting({ name = "Гость" }) {
  return <h1>Привет, {name}!</h1>;
}

<Greeting />           // "Привет, Гость!"
<Greeting name="Алекс" />  // "Привет, Алекс!"
```

### Условная передача props

Можно передавать props **условно**:

```jsx
function Button({ variant, size, disabled }) {
  return <button className={`btn btn-${variant} btn-${size}`} disabled={disabled}>Кнопка</button>;
}

// Передаём только нужные props
<Button variant="primary" size="large" />
<Button variant="secondary" disabled={true} />
```

### Передача всего объекта props

Можно передать **весь объект props** целиком:

```jsx
function UserCard(props) {
  return (
    <div>
      <h2>{props.name}</h2>
      <p>{props.email}</p>
    </div>
  );
}

// Использование
<UserCard name="Алекс" email="alex@example.com" />;
```

Но лучше использовать **деструктуризацию**:

```jsx
function UserCard({ name, email }) {
  return (
    <div>
      <h2>{name}</h2>
      <p>{email}</p>
    </div>
  );
}
```

### Передача данных через несколько уровней

Данные можно передавать через **несколько уровней компонентов**:

```jsx
function App() {
  const user = { name: "Алекс", email: "alex@example.com" };
  return <Layout user={user} />;
}

function Layout({ user }) {
  return <Header user={user} />;
}

function Header({ user }) {
  return <UserProfile user={user} />;
}

function UserProfile({ user }) {
  return <div>{user.name}</div>;
}
```

Это называется **prop drilling** (пробуривание props). Для глубокой вложенности лучше использовать Context API.

### ⚠️ Частая ошибка

Забывают фигурные скобки для не-строковых значений:

```jsx
// ❌ Неправильно
<Button count="5" />      // передаст строку "5"
<UserCard age="25" />     // передаст строку "25"

// ✅ Правильно
<Button count={5} />      // передаст число 5
<UserCard age={25} />     // передаст число 25
```

### Итоги

- Данные передаются через props (как атрибуты HTML)
- Строки можно передавать без `{}`, остальные типы — с `{}`
- Можно передавать любые типы данных (строки, числа, объекты, массивы, функции)
- Используй деструктуризацию для удобства
- Можно использовать spread оператор для передачи нескольких props
- Данные текут сверху вниз (от родителя к ребенку)

---

## 4. Как передать функцию в props

Функции в props передаются так же, как и другие значения — через props. Это позволяет компонентам **общаться с родителями** (вызывать функции, переданные сверху).

### Базовый синтаксис

```jsx
// Родительский компонент
function App() {
  const handleClick = () => {
    console.log("Кнопка нажата!");
  };

  return <Button onClick={handleClick} />;
}

// Дочерний компонент
function Button({ onClick }) {
  return <button onClick={onClick}>Нажми меня</button>;
}
```

Функция `handleClick` передаётся в `Button` через prop `onClick`.

### Зачем передавать функции?

Передача функций позволяет **дочерним компонентам обновлять состояние родителя**:

```jsx
function App() {
  const [count, setCount] = useState(0);

  const increment = () => {
    setCount(count + 1);
  };

  return (
    <div>
      <p>Счёт: {count}</p>
      <CounterButton onIncrement={increment} />
    </div>
  );
}

function CounterButton({ onIncrement }) {
  return <button onClick={onIncrement}>Увеличить</button>;
}
```

Когда кнопка нажимается, вызывается `increment` из `App`, и состояние обновляется.

### Разные способы передачи функций

#### 1. Именованная функция

```jsx
function App() {
  const handleSubmit = () => {
    console.log("Отправка формы");
  };

  return <Form onSubmit={handleSubmit} />;
}
```

#### 2. Стрелочная функция

```jsx
function App() {
  return <Form onSubmit={() => console.log("Отправка формы")} />;
}
```

#### 3. Inline функция (не рекомендуется для сложной логики)

```jsx
function App() {
  const [name, setName] = useState("");

  return <Input value={name} onChange={(e) => setName(e.target.value)} />;
}
```

### Передача параметров

Если функция должна принимать **параметры**, оборачивай её:

```jsx
function App() {
  const [items, setItems] = useState(["Элемент 1"]);

  const removeItem = (index) => {
    setItems(items.filter((_, i) => i !== index));
  };

  return (
    <div>
      {items.map((item, index) => (
        <Item
          key={index}
          text={item}
          onRemove={() => removeItem(index)} // Передаём index
        />
      ))}
    </div>
  );
}

function Item({ text, onRemove }) {
  return (
    <div>
      <span>{text}</span>
      <button onClick={onRemove}>Удалить</button>
    </div>
  );
}
```

### Передача данных через функцию

Функции могут **передавать данные обратно в родителя**:

```jsx
function App() {
  const [user, setUser] = useState(null);

  const handleUserUpdate = (newUser) => {
    setUser(newUser);
  };

  return <UserForm onUpdate={handleUserUpdate} />;
}

function UserForm({ onUpdate }) {
  const handleSubmit = (e) => {
    e.preventDefault();
    const formData = new FormData(e.target);
    const newUser = {
      name: formData.get("name"),
      email: formData.get("email"),
    };
    onUpdate(newUser); // Передаём данные в родителя
  };

  return (
    <form onSubmit={handleSubmit}>
      <input name="name" />
      <input name="email" />
      <button type="submit">Сохранить</button>
    </form>
  );
}
```

### Именование функций в props

Обычно функции в props называют с префиксом `on` или `handle`:

```jsx
<Button onClick={handleClick} />
<Form onSubmit={handleSubmit} />
<Input onChange={handleChange} />
<UserCard onEdit={handleEdit} />
```

Это **конвенция**, которая делает код понятнее.

### Обработка отсутствия функции

Иногда функция может быть не передана. Добавь проверку:

```jsx
function Button({ onClick, text }) {
  const handleClick = () => {
    if (onClick) {
      onClick();
    }
  };

  return <button onClick={handleClick}>{text}</button>;
}

// Или короче
function Button({ onClick, text }) {
  return <button onClick={onClick}>{text}</button>; // onClick может быть undefined
}
```

### ⚠️ Частая ошибка

Вызов функции сразу вместо передачи:

```jsx
// ❌ Неправильно
<Button onClick={handleClick()} />  // Функция вызовется сразу!

// ✅ Правильно
<Button onClick={handleClick} />    // Передаём функцию
```

**Разница:**

- `handleClick()` — вызов функции **сразу**
- `handleClick` — передача **ссылки на функцию**

### Ещё одна ошибка

Создание новой функции при каждом рендере (может вызвать лишние ре-рендеры):

```jsx
// ⚠️ Неоптимально
function App() {
  const [count, setCount] = useState(0);

  return (
    <Button
      onClick={() => setCount(count + 1)} // Новая функция при каждом рендере
    />
  );
}

// ✅ Лучше
function App() {
  const [count, setCount] = useState(0);

  const handleClick = () => {
    setCount(count + 1);
  };

  return <Button onClick={handleClick} />;
}

// ✅ Ещё лучше (с useCallback)
function App() {
  const [count, setCount] = useState(0);

  const handleClick = useCallback(() => {
    setCount(count + 1);
  }, [count]);

  return <Button onClick={handleClick} />;
}
```

### Итоги

- Функции передаются в props так же, как и другие значения
- Функции позволяют дочерним компонентам общаться с родителями
- Используй именование с префиксом `on` или `handle`
- Передавай ссылку на функцию (`onClick={handleClick}`), а не вызывай её (`onClick={handleClick()}`)
- Функции могут принимать параметры и передавать данные обратно в родителя

---

## 5. Что такое children

`children` — это **специальный prop**, который содержит всё, что находится между открывающим и закрывающим тегами компонента.

### Простое определение

`children` — это **содержимое компонента**.

```jsx
function Card({ children }) {
  return <div className="card">{children}</div>;
}

// Использование
<Card>
  <h2>Заголовок</h2>
  <p>Текст</p>
</Card>;
```

Всё, что находится между `<Card>` и `</Card>`, становится `children`.

### Что может быть children?

`children` может быть **любым содержимым**:

#### Текст

```jsx
function Button({ children }) {
  return <button>{children}</button>;
}

<Button>Нажми меня</Button>;
```

#### Элементы JSX

```jsx
function Container({ children }) {
  return <div className="container">{children}</div>;
}

<Container>
  <h1>Заголовок</h1>
  <p>Текст</p>
</Container>;
```

#### Несколько элементов

```jsx
function Card({ children }) {
  return <div className="card">{children}</div>;
}

<Card>
  <h2>Заголовок</h2>
  <p>Описание</p>
  <button>Действие</button>
</Card>;
```

#### Другие компоненты

```jsx
function Layout({ children }) {
  return (
    <div>
      <Header />
      <main>{children}</main>
      <Footer />
    </div>
  );
}

<Layout>
  <UserProfile />
  <PostsList />
</Layout>;
```

#### Выражения

```jsx
function Greeting({ children }) {
  return <h1>{children}</h1>;
}

<Greeting>Привет, {name}!</Greeting>;
```

### Явное использование children

`children` можно использовать **явно**:

```jsx
function Card(props) {
  return <div className="card">{props.children}</div>;
}
```

Или **неявно** через деструктуризацию:

```jsx
function Card({ children }) {
  return <div className="card">{children}</div>;
}
```

Оба варианта работают одинаково.

### Другие props вместе с children

Можно использовать `children` вместе с **другими props**:

```jsx
function Card({ title, children }) {
  return (
    <div className="card">
      {title && <h2>{title}</h2>}
      <div className="card-body">{children}</div>
    </div>
  );
}

// Использование
<Card title="Заголовок">
  <p>Содержимое карточки</p>
</Card>;
```

### Дети могут отсутствовать

`children` может быть `undefined`, если компонент **самозакрывающийся**:

```jsx
<Card />; // children будет undefined

function Card({ children }) {
  return <div className="card">{children || <p>Нет содержимого</p>}</div>;
}
```

### Множественные children

`children` может быть **массивом**, если передано несколько элементов:

```jsx
function List({ children }) {
  return (
    <ul>
      {React.Children.map(children, (child, index) => (
        <li key={index}>{child}</li>
      ))}
    </ul>
  );
}

<List>
  <span>Пункт 1</span>
  <span>Пункт 2</span>
  <span>Пункт 3</span>
</List>;
```

Для работы с несколькими children используй `React.Children`.

### Визуальная аналогия

`children` — как **содержимое коробки**:

- Коробка (компонент) может быть разной
- Но содержимое (children) определяется тем, что туда положили

```jsx
<Box>🎁 Содержимое коробки</Box>
```

### Итоги

- `children` — специальный prop, содержащий содержимое компонента
- `children` — это всё, что находится между открывающим и закрывающим тегами
- `children` может быть текстом, элементами, компонентами, выражениями
- Можно использовать вместе с другими props
- `children` может отсутствовать (undefined)

---

## 6. Как типично используют children

`children` используется для создания **переиспользуемых компонентов-обёрток**, которые могут содержать любое содержимое.

### Паттерн 1: Компоненты-обёртки (Layout)

Самый распространённый случай — **компоненты-обёртки** для структуры страницы:

```jsx
function Layout({ children }) {
  return (
    <div className="layout">
      <Header />
      <main>{children}</main>
      <Footer />
    </div>
  );
}

// Использование
<Layout>
  <HomePage />
</Layout>

<Layout>
  <AboutPage />
</Layout>
```

Один компонент `Layout`, разное содержимое.

### Паттерн 2: UI-компоненты (Card, Modal, Button)

Компоненты, которые **оборачивают содержимое**:

```jsx
function Card({ children, title }) {
  return (
    <div className="card">
      {title && <h2 className="card-title">{title}</h2>}
      <div className="card-body">{children}</div>
    </div>
  );
}

// Использование
<Card title="Пользователь">
  <p>Имя: Алекс</p>
  <p>Email: alex@example.com</p>
</Card>

<Card title="Статья">
  <p>Содержимое статьи...</p>
</Card>
```

### Паттерн 3: Кнопки с содержимым

```jsx
function Button({ children, onClick, variant = "primary" }) {
  return (
    <button
      className={`btn btn-${variant}`}
      onClick={onClick}
    >
      {children}
    </button>
  );
}

// Использование
<Button onClick={handleClick}>Сохранить</Button>
<Button variant="danger" onClick={handleDelete}>
  <span>🗑️</span> Удалить
</Button>
```

### Паттерн 4: Модальные окна

```jsx
function Modal({ isOpen, onClose, children, title }) {
  if (!isOpen) return null;

  return (
    <div className="modal-overlay" onClick={onClose}>
      <div className="modal" onClick={(e) => e.stopPropagation()}>
        <div className="modal-header">
          <h2>{title}</h2>
          <button onClick={onClose}>×</button>
        </div>
        <div className="modal-body">{children}</div>
      </div>
    </div>
  );
}

// Использование
<Modal isOpen={isOpen} onClose={() => setIsOpen(false)} title="Подтверждение">
  <p>Вы уверены, что хотите удалить этот элемент?</p>
  <button onClick={handleDelete}>Да, удалить</button>
</Modal>;
```

### Паттерн 5: Условное отображение

Компоненты, которые **условно отображают** содержимое:

```jsx
function Conditional({ condition, children }) {
  return condition ? <>{children}</> : null;
}

// Использование
<Conditional condition={isLoggedIn}>
  <UserMenu />
</Conditional>;
```

### Паттерн 6: Стилизованные контейнеры

```jsx
function Container({ children, maxWidth = "1200px" }) {
  return (
    <div style={{ maxWidth, margin: "0 auto", padding: "20px" }}>
      {children}
    </div>
  );
}

// Использование
<Container>
  <h1>Заголовок</h1>
  <p>Содержимое</p>
</Container>;
```

### Паттерн 7: Композиция компонентов

Комбинирование нескольких компонентов с `children`:

```jsx
function Page({ children }) {
  return (
    <Container>
      <Layout>
        <Card>{children}</Card>
      </Layout>
    </Container>
  );
}

// Использование
<Page>
  <h1>Заголовок страницы</h1>
  <p>Содержимое</p>
</Page>;
```

### Паттерн 8: Передача компонентов как children

Иногда `children` — это **функция или компонент**:

```jsx
function Tabs({ children, tabs }) {
  const [activeTab, setActiveTab] = useState(0);

  return (
    <div>
      <div className="tabs">
        {tabs.map((tab, index) => (
          <button
            key={index}
            onClick={() => setActiveTab(index)}
            className={activeTab === index ? "active" : ""}
          >
            {tab}
          </button>
        ))}
      </div>
      <div className="tab-content">{children[activeTab]}</div>
    </div>
  );
}

// Использование
<Tabs tabs={["Вкладка 1", "Вкладка 2"]}>
  <div>Содержимое вкладки 1</div>
  <div>Содержимое вкладки 2</div>
</Tabs>;
```

### Паттерн 9: Обработка пустого children

```jsx
function Card({ children, title, emptyMessage = "Нет содержимого" }) {
  return (
    <div className="card">
      {title && <h2>{title}</h2>}
      <div className="card-body">
        {children || <p className="empty">{emptyMessage}</p>}
      </div>
    </div>
  );
}

// Использование
<Card title="Список">
  {items.length > 0
    ? items.map((item) => <Item key={item.id} item={item} />)
    : null}
</Card>;
```

### Преимущества использования children

1. **Переиспользуемость** — один компонент, разное содержимое
2. **Гибкость** — содержимое определяется при использовании
3. **Композиция** — легко комбинировать компоненты
4. **Читаемость** — код понятнее, структура видна сразу

### Итоги

- `children` используется для создания переиспользуемых компонентов-обёрток
- Типичные случаи: Layout, Card, Modal, Button, Container
- Позволяет создавать гибкие и композируемые компоненты
- Содержимое определяется при использовании компонента
- Можно комбинировать несколько компонентов с children

---

## 7. Что будет, если изменить props напрямую

Если попытаться изменить props напрямую, **React выдаст предупреждение** (в строгом режиме), и изменения **не применятся**.

### Что произойдёт?

#### В строгом режиме (Strict Mode)

```jsx
function BadComponent({ name }) {
  name = "Другое имя"; // ❌ Попытка изменить prop
  return <h1>{name}</h1>;
}
```

React выдаст **предупреждение**:

```
Warning: Cannot assign to read only property 'name' of object
```

#### В обычном режиме

Предупреждение может не появиться, но изменения **всё равно не применятся**:

```jsx
function Component({ user }) {
  user.name = "Новое имя"; // ⚠️ Технически возможно, но неправильно
  return <div>{user.name}</div>;
}
```

Это **не вызовет ошибку**, но нарушит принципы React.

### Почему это плохо?

#### 1. Нарушение принципа одностороннего потока данных

Данные в React должны течь **только сверху вниз**:

```
Родитель → Ребёнок (props)
```

Изменение props **снизу** нарушает этот принцип.

#### 2. Непредсказуемое поведение

Если компонент изменяет props, его поведение становится **непредсказуемым**:

```jsx
function Component({ items }) {
  items.push("Новый элемент"); // ❌ Изменение массива
  return <div>{items.length}</div>;
}

// Использование
const items = ["Элемент 1"];
<Component items={items} />;
console.log(items); // ["Элемент 1", "Новый элемент"] ⚠️
```

Изменение в дочернем компоненте **повлияло на родительский**!

#### 3. Проблемы с ре-рендерингом

React может **не заметить изменения** и не обновить компонент:

```jsx
function Component({ user }) {
  user.name = "Новое имя"; // Изменение объекта
  // React может не обновить компонент, т.к. ссылка на объект не изменилась
  return <div>{user.name}</div>;
}
```

### Правильный подход

Если нужно изменить данные, делай это в **родительском компоненте**:

```jsx
// ✅ Правильно
function App() {
  const [user, setUser] = useState({ name: "Алекс", age: 25 });

  const updateName = (newName) => {
    setUser({ ...user, name: newName });
  };

  return <UserCard user={user} onUpdateName={updateName} />;
}

function UserCard({ user, onUpdateName }) {
  return (
    <div>
      <p>{user.name}</p>
      <button onClick={() => onUpdateName("Новое имя")}>Изменить имя</button>
    </div>
  );
}
```

### Что происходит с объектами и массивами?

**Важно:** хотя сам props-объект неизменяем, **внутренности объектов и массивов** технически можно изменить:

```jsx
function Component({ user }) {
  user.name = "Новое имя"; // ⚠️ Технически работает, но НЕПРАВИЛЬНО
  return <div>{user.name}</div>;
}
```

Это **не вызовет ошибку**, но:

- Нарушит принципы React
- Может привести к багам
- Сделает код непредсказуемым

### Строгий режим и предупреждения

В **строгом режиме** React пытается предотвратить мутации:

```jsx
function Component({ items }) {
  items.push("Новый"); // ⚠️ Предупреждение в Strict Mode
  return <div>{items.length}</div>;
}
```

Но это работает **не всегда**, особенно для вложенных объектов.

### ⚠️ Частая ошибка

Попытка изменить props при инициализации:

```jsx
// ❌ Неправильно
function Component({ user }) {
  // Попытка "нормализовать" данные
  if (!user.email) {
    user.email = "Нет email"; // Мутация!
  }

  return <div>{user.email}</div>;
}

// ✅ Правильно
function Component({ user }) {
  const email = user.email || "Нет email"; // Новая переменная
  return <div>{email}</div>;
}
```

### Итоги

- Изменение props напрямую вызывает предупреждение в строгом режиме
- Изменения не применяются или работают неправильно
- Нарушает принцип одностороннего потока данных
- Может привести к непредсказуемому поведению
- Правильный подход — изменять данные в родительском компоненте
- Используй функции, переданные через props, для обновления данных

---
