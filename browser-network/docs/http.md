# HTTP — Протокол передачи данных

Протокол HTTP (HyperText Transfer Protocol) и его особенности.  
Материал представлен в формате **вопрос–ответ**.

---

## Содержание (Table of Contents)

1. [Что такое HTTP](#1-что-такое-http)
2. [HTTP vs HTTPS](#2-http-vs-https)
3. [HTTP методы (GET, POST, PUT, PATCH, DELETE)](#3-http-методы-get-post-put-patch-delete)
4. [Разница PUT vs PATCH](#4-разница-put-vs-patch)
5. [Idempotent запросы](#5-idempotent-запросы)
6. [HTTP статус-коды (2xx, 3xx, 4xx, 5xx)](#6-http-статус-коды-2xx-3xx-4xx-5xx)
7. [Разница 401 vs 403](#7-разница-401-vs-403)

---

## 1. Что такое HTTP

HTTP (HyperText Transfer Protocol) — это **протокол передачи данных** в сети Интернет, который используется для обмена информацией между клиентом (браузером) и сервером.

### Простое определение

HTTP — это "язык", на котором браузер **разговаривает с сервером**, запрашивая веб-страницы, изображения, видео и другие ресурсы, а сервер отправляет эти данные в ответ.

### Как работает HTTP

HTTP работает по модели **запрос-ответ** (request-response):

1. **Клиент** отправляет HTTP-запрос
2. **Сервер** обрабатывает запрос
3. **Сервер** отправляет HTTP-ответ
4. **Соединение закрывается** (в HTTP/1.0 и HTTP/1.1)

### Структура HTTP-запроса

```
GET /index.html HTTP/1.1
Host: example.com
User-Agent: Mozilla/5.0
Accept: text/html

(тело запроса, если есть)
```

**Составляющие:**
- **Метод** (`GET`, `POST`, `PUT`, и т.д.)
- **Путь** (`/index.html`)
- **Версия протокола** (`HTTP/1.1`)
- **Заголовки** (`Host`, `User-Agent`, `Accept`)
- **Тело** (опционально, для `POST`, `PUT`, `PATCH`)

### Структура HTTP-ответа

```
HTTP/1.1 200 OK
Content-Type: text/html
Content-Length: 1234

<html>
  <body>Привет!</body>
</html>
```

**Составляющие:**
- **Статус-код** (`200 OK`)
- **Заголовки** (`Content-Type`, `Content-Length`)
- **Тело ответа** (HTML, JSON, изображение и т.д.)

### Пример HTTP-запроса в JavaScript

```javascript
// Fetch API
fetch('https://api.example.com/users')
  .then(response => response.json())
  .then(data => console.log(data));

// XMLHttpRequest (старый способ)
const xhr = new XMLHttpRequest();
xhr.open('GET', 'https://api.example.com/users');
xhr.send();
xhr.onload = function() {
  console.log(xhr.response);
};
```

### Визуальная аналогия

HTTP — как **почтовая служба**:

- **Запрос** — письмо с адресом получателя и содержимым
- **Сервер** — почтовое отделение, которое обрабатывает письмо
- **Ответ** — посылка с нужной информацией
- **Протокол** — правила оформления письма (куда писать адрес, как упаковывать)

### Основные характеристики HTTP

#### 1. Без состояния (Stateless)

HTTP **не запоминает** предыдущие запросы. Каждый запрос независим:

```javascript
// Первый запрос
fetch('/api/login', { method: 'POST', body: JSON.stringify({ user: 'alex' }) });

// Второй запрос — сервер НЕ ЗНАЕТ о первом
// Нужны cookies или токены для сохранения состояния
fetch('/api/profile'); // Без токена сервер не поймёт, кто запрашивает
```

#### 2. Текстовый протокол

HTTP использует **текстовый формат** (хотя тело может быть бинарным):

```
GET /page HTTP/1.1    ← Текст
Host: example.com     ← Текст
```

#### 3. Клиент-серверная архитектура

- **Клиент** инициирует запрос
- **Сервер** обрабатывает и отвечает
- Роли четко разделены

### Версии HTTP

#### HTTP/1.0 (1996)
- Базовые возможности
- Одно соединение на запрос

#### HTTP/1.1 (1997)
- Keep-Alive (повторное использование соединения)
- Кеширование
- Потоковая передача (chunked transfer)

#### HTTP/2 (2015)
- Мультиплексирование (несколько запросов в одном соединении)
- Сжатие заголовков (HPACK)
- Приоритизация запросов

#### HTTP/3 (2020)
- Использует QUIC (на базе UDP)
- Меньше задержек
- Лучше работает при потере пакетов

### ⚠️ Частая ошибка

Думают, что HTTP и HTTPS — разные протоколы:

```javascript
// ❌ Неправильное понимание
// HTTP — один протокол
// HTTPS — другой протокол

// ✅ Правильное понимание
// HTTPS — это HTTP + TLS/SSL шифрование
// Это тот же HTTP, но защищённый
```

### Итоги

- HTTP — протокол для передачи данных между клиентом и сервером
- Работает по модели запрос-ответ
- Без состояния (stateless) — каждый запрос независим
- Текстовый протокол (хотя тело может быть бинарным)
- Имеет несколько версий: 1.0, 1.1, 2, 3
- HTTPS — это HTTP с шифрованием (TLS/SSL)

---

## 2. HTTP vs HTTPS

HTTPS (HTTP Secure) — это **HTTP с шифрованием**, обеспечивающий безопасную передачу данных между клиентом и сервером.

### Простое определение

Если HTTP — это "обычная почта" (видна всем), то HTTPS — это "заказное письмо в сейфе" (зашифровано, никто не может прочитать содержимое).

### Основные отличия

| Характеристика | HTTP | HTTPS |
|---------------|------|-------|
| **Порт** | 80 | 443 |
| **Шифрование** | ❌ Нет | ✅ Да (TLS/SSL) |
| **Безопасность** | ❌ Данные передаются открыто | ✅ Данные зашифрованы |
| **Сертификат** | ❌ Не нужен | ✅ Нужен SSL-сертификат |
| **Производительность** | ⚡ Быстрее | 🐌 Чуть медленнее (из-за шифрования) |
| **SEO** | ⚠️ Поисковики предпочитают HTTPS | ✅ Лучше для SEO |

### Как работает HTTPS

#### 1. SSL/TLS рукопожатие (handshake)

```
Клиент                    Сервер
   |                         |
   |--- Client Hello ------->|
   |                         |
   |<-- Server Hello --------|
   |<-- Certificate ---------|
   |<-- Server Hello Done ---|
   |                         |
   |--- Client Key Exchange->|
   |--- Change Cipher Spec ->|
   |--- Finished ----------->|
   |                         |
   |<-- Change Cipher Spec --|
   |<-- Finished ------------|
   |                         |
   |<====== Шифрованное соединение =====>|
```

**Процесс:**
1. Клиент отправляет `Client Hello`
2. Сервер отправляет сертификат
3. Клиент проверяет сертификат
4. Обмен ключами шифрования
5. Установка шифрованного соединения

#### 2. Шифрование данных

После установления соединения все данные шифруются:

```
HTTP (открытый текст):
GET /login HTTP/1.1
Password: mypassword123

HTTPS (зашифрованный):
a8f3d9b2c1e4f6a7b8c9d0e1f2a3b4c5d6e7f8a9b0c1d2e3f4a5b6c7d8e9f0a1b2...
```

### Сертификаты SSL/TLS

HTTPS требует **SSL-сертификат**, который подтверждает подлинность сервера.

#### Типы сертификатов

1. **DV (Domain Validation)** — проверка домена
   - Быстрое получение
   - Низкая стоимость (часто бесплатно)
   - Базовый уровень доверия

2. **OV (Organization Validation)** — проверка организации
   - Проверка юридического лица
   - Средний уровень доверия

3. **EV (Extended Validation)** — расширенная проверка
   - Полная проверка организации
   - Высокий уровень доверия
   - Зелёная строка в адресной строке (в старых браузерах)

#### Бесплатные сертификаты

```bash
# Let's Encrypt — бесплатный SSL-сертификат
certbot --nginx -d example.com
```

### Примеры использования

#### HTTP (небезопасно)

```javascript
// ❌ Пароль передаётся в открытом виде
fetch('http://api.example.com/login', {
  method: 'POST',
  body: JSON.stringify({
    username: 'alex',
    password: 'secret123'  // Видно всем!
  })
});
```

**Риски:**
- Пароли можно перехватить
- Данные можно изменить
- Подделка сайта (MITM-атака)

#### HTTPS (безопасно)

```javascript
// ✅ Пароль зашифрован
fetch('https://api.example.com/login', {
  method: 'POST',
  body: JSON.stringify({
    username: 'alex',
    password: 'secret123'  // Зашифровано!
  })
});
```

**Преимущества:**
- Данные защищены от перехвата
- Подтверждена подлинность сервера
- Защита от MITM-атак

### Визуальная аналогия

**HTTP** — как **открытка**:
- Все могут прочитать содержимое
- Можно изменить текст
- Нельзя быть уверенным, что письмо от того, от кого кажется

**HTTPS** — как **запечатанный конверт с печатью**:
- Никто не может прочитать содержимое
- Нельзя изменить без обнаружения
- Печать подтверждает отправителя

### Смешанный контент (Mixed Content)

⚠️ **Проблема:** HTTPS-страница загружает HTTP-ресурсы

```html
<!-- ❌ Плохо: HTTPS страница, но HTTP ресурс -->
<script src="http://example.com/script.js"></script>
<img src="http://example.com/image.jpg" />

<!-- ✅ Хорошо: Все ресурсы по HTTPS -->
<script src="https://example.com/script.js"></script>
<img src="https://example.com/image.jpg" />
```

**Браузеры блокируют** смешанный контент для безопасности.

### Производительность

HTTPS **немного медленнее** HTTP из-за:
- Шифрования/дешифрования данных
- SSL/TLS handshake (дополнительные запросы)

**Но разница минимальна:**
- Современные процессоры быстро шифруют
- HTTP/2 работает только с HTTPS
- Безопасность важнее небольшой задержки

### Итоги

- HTTPS — это HTTP с шифрованием (TLS/SSL)
- Порт HTTPS — 443 (HTTP — 80)
- Требует SSL-сертификат
- Защищает данные от перехвата и изменения
- Подтверждает подлинность сервера
- Немного медленнее HTTP, но безопасность важнее
- Все современные сайты должны использовать HTTPS

---

## 3. HTTP методы (GET, POST, PUT, PATCH, DELETE)

HTTP методы (или HTTP verbs) определяют **тип операции**, которую клиент хочет выполнить с ресурсом на сервере.

### Простое определение

HTTP методы — это "глаголы", которые говорят серверу, **что делать** с ресурсом:
- `GET` — "дать мне" (получить)
- `POST` — "создать" (добавить)
- `PUT` — "заменить" (обновить всё)
- `PATCH` — "изменить частично" (обновить часть)
- `DELETE` — "удалить"

### GET — получение данных

**Назначение:** Получить данные с сервера (чтение)

**Характеристики:**
- ✅ Безопасный (safe) — не изменяет данные
- ✅ Идемпотентный (idempotent)
- 📤 Параметры в URL (query string)
- ❌ Не имеет тела запроса (body)

#### Примеры

```javascript
// Получить список пользователей
fetch('https://api.example.com/users');

// С параметрами
fetch('https://api.example.com/users?page=1&limit=10');

// Получить конкретного пользователя
fetch('https://api.example.com/users/123');
```

**HTTP-запрос:**
```
GET /users?page=1&limit=10 HTTP/1.1
Host: api.example.com
```

**HTTP-ответ:**
```
HTTP/1.1 200 OK
Content-Type: application/json

[
  { "id": 1, "name": "Алекс" },
  { "id": 2, "name": "Мария" }
]
```

### POST — создание ресурса

**Назначение:** Создать новый ресурс

**Характеристики:**
- ❌ Не безопасный (изменяет данные)
- ❌ Не идемпотентный (повторный запрос создаст новый ресурс)
- 📦 Данные в теле запроса (body)
- ✅ Может возвращать созданный ресурс

#### Примеры

```javascript
// Создать нового пользователя
fetch('https://api.example.com/users', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    name: 'Алекс',
    email: 'alex@example.com'
  })
});

// Отправить форму
fetch('https://api.example.com/login', {
  method: 'POST',
  body: new FormData(formElement)
});
```

**HTTP-запрос:**
```
POST /users HTTP/1.1
Host: api.example.com
Content-Type: application/json

{
  "name": "Алекс",
  "email": "alex@example.com"
}
```

**HTTP-ответ:**
```
HTTP/1.1 201 Created
Content-Type: application/json
Location: /users/123

{
  "id": 123,
  "name": "Алекс",
  "email": "alex@example.com"
}
```

### PUT — полная замена ресурса

**Назначение:** Заменить ресурс целиком (или создать, если не существует)

**Характеристики:**
- ❌ Не безопасный
- ✅ Идемпотентный (повторный запрос даст тот же результат)
- 📦 Данные в теле запроса
- 🔄 Заменяет весь ресурс

#### Примеры

```javascript
// Заменить пользователя целиком
fetch('https://api.example.com/users/123', {
  method: 'PUT',
  headers: {
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    name: 'Алексей',      // Изменено
    email: 'alex@example.com',
    age: 25               // Новое поле
  })
});
```

**HTTP-запрос:**
```
PUT /users/123 HTTP/1.1
Host: api.example.com
Content-Type: application/json

{
  "name": "Алексей",
  "email": "alex@example.com",
  "age": 25
}
```

**Важно:** Если поле не указано — оно **удаляется**:

```javascript
// Было: { name: "Алекс", email: "alex@example.com", age: 30, city: "Москва" }
// Отправили: { name: "Алексей", email: "alex@example.com" }
// Стало: { name: "Алексей", email: "alex@example.com" }
// age и city удалены!
```

### PATCH — частичное обновление

**Назначение:** Изменить только указанные поля ресурса

**Характеристики:**
- ❌ Не безопасный
- ⚠️ Не всегда идемпотентный (зависит от реализации)
- 📦 Данные в теле запроса
- 🔧 Обновляет только указанные поля

#### Примеры

```javascript
// Изменить только имя
fetch('https://api.example.com/users/123', {
  method: 'PATCH',
  headers: {
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    name: 'Алексей'  // Только это поле обновится
  })
});
```

**HTTP-запрос:**
```
PATCH /users/123 HTTP/1.1
Host: api.example.com
Content-Type: application/json

{
  "name": "Алексей"
}
```

**Результат:** Остальные поля остаются без изменений.

### DELETE — удаление ресурса

**Назначение:** Удалить ресурс

**Характеристики:**
- ❌ Не безопасный
- ✅ Идемпотентный (удаление уже удалённого — тот же результат)
- ❌ Обычно не имеет тела запроса
- 🗑️ Удаляет ресурс

#### Примеры

```javascript
// Удалить пользователя
fetch('https://api.example.com/users/123', {
  method: 'DELETE'
});
```

**HTTP-запрос:**
```
DELETE /users/123 HTTP/1.1
Host: api.example.com
```

**HTTP-ответ:**
```
HTTP/1.1 204 No Content
```

### Сравнительная таблица

| Метод | Назначение | Безопасный | Идемпотентный | Тело запроса | Пример URL |
|-------|-----------|-----------|---------------|--------------|------------|
| `GET` | Получить | ✅ | ✅ | ❌ | `/users/123` |
| `POST` | Создать | ❌ | ❌ | ✅ | `/users` |
| `PUT` | Заменить | ❌ | ✅ | ✅ | `/users/123` |
| `PATCH` | Частично обновить | ❌ | ⚠️ | ✅ | `/users/123` |
| `DELETE` | Удалить | ❌ | ✅ | ❌ | `/users/123` |

### Визуальная аналогия

HTTP методы — как **действия с книгой в библиотеке**:

- **GET** — "Дайте мне книгу" (прочитать, не изменяя)
- **POST** — "Добавьте новую книгу" (создать)
- **PUT** — "Замените всю книгу новой версией" (заменить целиком)
- **PATCH** — "Исправьте только эту страницу" (изменить часть)
- **DELETE** — "Удалите эту книгу" (удалить)

### ⚠️ Частая ошибка

Используют `GET` для изменения данных:

```javascript
// ❌ Плохо: изменение через GET
fetch('/api/users/123/delete');  // GET запрос удаляет!

// ✅ Хорошо: изменение через правильный метод
fetch('/api/users/123', { method: 'DELETE' });
```

**Почему плохо:**
- GET запросы кешируются
- GET запросы логируются в истории
- GET запросы могут выполняться автоматически
- Нарушает семантику HTTP

### Другие методы

#### HEAD — получить только заголовки

```javascript
// Как GET, но без тела ответа
fetch('/api/users/123', { method: 'HEAD' });
```

#### OPTIONS — узнать доступные методы

```javascript
// Используется для CORS preflight
fetch('/api/users', { method: 'OPTIONS' });
```

#### TRACE — диагностика (редко используется)

### Итоги

- `GET` — получить данные (безопасный, идемпотентный)
- `POST` — создать ресурс (не безопасный, не идемпотентный)
- `PUT` — заменить ресурс целиком (идемпотентный)
- `PATCH` — частично обновить ресурс
- `DELETE` — удалить ресурс (идемпотентный)
- Методы определяют семантику операции
- Важно использовать правильный метод для правильной операции

---

## 4. Разница PUT vs PATCH

`PUT` и `PATCH` — оба используются для обновления ресурсов, но работают **по-разному**: `PUT` заменяет ресурс целиком, а `PATCH` обновляет только указанные поля.

### Простое определение

- **PUT** — "Замени всю книгу"
- **PATCH** — "Исправь только эту страницу"

### PUT — полная замена

`PUT` **заменяет весь ресурс** на новые данные. Если поле не указано — оно **удаляется**.

#### Пример с PUT

**Было на сервере:**
```json
{
  "id": 123,
  "name": "Алекс",
  "email": "alex@example.com",
  "age": 30,
  "city": "Москва"
}
```

**PUT запрос:**
```javascript
fetch('https://api.example.com/users/123', {
  method: 'PUT',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    name: 'Алексей',
    email: 'alexey@example.com'
    // age и city НЕ указаны!
  })
});
```

**Стало на сервере:**
```json
{
  "id": 123,
  "name": "Алексей",
  "email": "alexey@example.com"
  // age и city УДАЛЕНЫ!
}
```

### PATCH — частичное обновление

`PATCH` **обновляет только указанные поля**. Остальные поля остаются без изменений.

#### Пример с PATCH

**Было на сервере:**
```json
{
  "id": 123,
  "name": "Алекс",
  "email": "alex@example.com",
  "age": 30,
  "city": "Москва"
}
```

**PATCH запрос:**
```javascript
fetch('https://api.example.com/users/123', {
  method: 'PATCH',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    name: 'Алексей'
    // Обновляем только name
  })
});
```

**Стало на сервере:**
```json
{
  "id": 123,
  "name": "Алексей",        // Изменено
  "email": "alex@example.com",  // Без изменений
  "age": 30,                 // Без изменений
  "city": "Москва"           // Без изменений
}
```

### Сравнительная таблица

| Характеристика | PUT | PATCH |
|---------------|-----|-------|
| **Что делает** | Заменяет весь ресурс | Обновляет указанные поля |
| **Неуказанные поля** | Удаляются | Остаются без изменений |
| **Идемпотентность** | ✅ Всегда идемпотентный | ⚠️ Зависит от реализации |
| **Когда использовать** | Когда знаешь все поля | Когда нужно обновить часть |

### Визуальная аналогия

**PUT** — как **переписать всю книгу заново**:
- Берёшь чистый лист
- Пишешь только нужные главы
- Остальные главы исчезают

**PATCH** — как **исправить опечатки в книге**:
- Берёшь существующую книгу
- Исправляешь только указанные места
- Остальное остаётся как было

### Практические примеры

#### Пример 1: Обновление профиля пользователя

**PUT (неправильно для этого случая):**
```javascript
// ❌ Нужно знать ВСЕ поля, иначе потеряем данные
fetch('/api/users/123', {
  method: 'PUT',
  body: JSON.stringify({
    name: 'Алексей',
    email: 'alexey@example.com'
    // Если забыли указать age и city — они удалятся!
  })
});
```

**PATCH (правильно):**
```javascript
// ✅ Обновляем только имя, остальное не трогаем
fetch('/api/users/123', {
  method: 'PATCH',
  body: JSON.stringify({
    name: 'Алексей'
  })
});
```

#### Пример 2: Создание/замена ресурса

**PUT (правильно, если нужно заменить целиком):**
```javascript
// ✅ Заменяем весь документ
fetch('/api/documents/123', {
  method: 'PUT',
  body: JSON.stringify({
    title: 'Новый заголовок',
    content: 'Новое содержимое',
    author: 'Алекс'
  })
});
```

### Идемпотентность

#### PUT — всегда идемпотентный

```javascript
// Первый запрос
PUT /users/123 { name: "Алексей", email: "alexey@example.com" }
// Результат: { name: "Алексей", email: "alexey@example.com" }

// Повторный запрос (тот же)
PUT /users/123 { name: "Алексей", email: "alexey@example.com" }
// Результат: { name: "Алексей", email: "alexey@example.com" } (тот же!)

// ✅ Идемпотентный
```

#### PATCH — может быть не идемпотентным

```javascript
// Первый запрос: увеличить счетчик
PATCH /articles/123 { views: views + 1 }
// Результат: views = 101

// Повторный запрос (тот же)
PATCH /articles/123 { views: views + 1 }
// Результат: views = 102 (другой!)

// ❌ Не идемпотентный (но это зависит от реализации)
```

**Чтобы PATCH был идемпотентным:**
```javascript
// Установить конкретное значение (идемпотентно)
PATCH /articles/123 { views: 101 }
PATCH /articles/123 { views: 101 }  // Тот же результат

// ✅ Идемпотентный
```

### ⚠️ Частая ошибка

Используют PUT для частичного обновления:

```javascript
// ❌ Плохо: PUT для частичного обновления
fetch('/api/users/123', {
  method: 'PUT',
  body: JSON.stringify({
    name: 'Алексей'
    // Забыли указать email, age, city — они удалятся!
  })
});

// ✅ Хорошо: PATCH для частичного обновления
fetch('/api/users/123', {
  method: 'PATCH',
  body: JSON.stringify({
    name: 'Алексей'
    // Остальные поля не трогаются
  })
});
```

### Когда использовать что?

#### Используй PUT, когда:
- ✅ Нужно заменить ресурс **целиком**
- ✅ Знаешь **все поля** ресурса
- ✅ Хочешь **гарантированную идемпотентность**
- ✅ Создаёшь ресурс с известным ID

#### Используй PATCH, когда:
- ✅ Нужно обновить **только некоторые поля**
- ✅ Не знаешь все поля ресурса
- ✅ Хочешь избежать потери данных
- ✅ Обновляешь форму с несколькими полями

### Итоги

- `PUT` заменяет ресурс целиком (неуказанные поля удаляются)
- `PATCH` обновляет только указанные поля (остальные остаются)
- `PUT` всегда идемпотентный
- `PATCH` может быть не идемпотентным (зависит от реализации)
- Используй `PUT` для полной замены, `PATCH` для частичного обновления
- `PATCH` безопаснее, когда не знаешь все поля ресурса

---

## 5. Idempotent запросы

Идемпотентность (idempotence) — это свойство операции, при котором **повторное выполнение даёт тот же результат**, что и однократное выполнение.

### Простое определение

Идемпотентный запрос — это запрос, который можно выполнить **много раз**, и результат будет **одинаковым**.

**Аналогия:** Нажать кнопку включения света (уже включённого) — свет останется включённым. Это идемпотентно.

### Что означает идемпотентность

#### Математическое определение

В математике функция `f` идемпотентна, если:
```
f(f(x)) = f(x)
```

**Пример:**
```javascript
Math.abs(Math.abs(-5)) === Math.abs(-5)  // true
// Math.abs идемпотентна
```

#### В контексте HTTP

HTTP-запрос идемпотентен, если:
- Повторный запрос с теми же данными даёт **тот же результат**
- Повторный запрос не изменяет состояние сервера **дополнительно**

### Идемпотентные HTTP методы

| Метод | Идемпотентный? | Почему |
|-------|----------------|--------|
| `GET` | ✅ Да | Только читает, не изменяет |
| `PUT` | ✅ Да | Заменяет ресурс, повторный запрос — та же замена |
| `DELETE` | ✅ Да | Удаление уже удалённого — тот же результат |
| `PATCH` | ⚠️ Зависит | Может быть идемпотентным, если устанавливает значение |
| `POST` | ❌ Нет | Каждый запрос создаёт новый ресурс |

### Примеры идемпотентных запросов

#### GET — всегда идемпотентный

```javascript
// Первый запрос
GET /users/123
// Ответ: { id: 123, name: "Алекс" }

// Повторный запрос (тот же)
GET /users/123
// Ответ: { id: 123, name: "Алекс" } (тот же!)

// ✅ Идемпотентный
```

#### PUT — идемпотентный

```javascript
// Первый запрос
PUT /users/123
Body: { name: "Алексей", email: "alexey@example.com" }
// Результат: { id: 123, name: "Алексей", email: "alexey@example.com" }

// Повторный запрос (тот же)
PUT /users/123
Body: { name: "Алексей", email: "alexey@example.com" }
// Результат: { id: 123, name: "Алексей", email: "alexey@example.com" } (тот же!)

// ✅ Идемпотентный
```

#### DELETE — идемпотентный

```javascript
// Первый запрос
DELETE /users/123
// Результат: 204 No Content (пользователь удалён)

// Повторный запрос
DELETE /users/123
// Результат: 404 Not Found или 204 No Content (пользователь уже удалён)

// ✅ Идемпотентный (результат одинаковый — пользователя нет)
```

### Примеры неидемпотентных запросов

#### POST — не идемпотентный

```javascript
// Первый запрос
POST /users
Body: { name: "Алекс", email: "alex@example.com" }
// Результат: { id: 124, name: "Алекс", email: "alex@example.com" }

// Повторный запрос (тот же)
POST /users
Body: { name: "Алекс", email: "alex@example.com" }
// Результат: { id: 125, name: "Алекс", email: "alex@example.com" } (НОВЫЙ пользователь!)

// ❌ Не идемпотентный (каждый запрос создаёт новый ресурс)
```

#### PATCH — может быть не идемпотентным

```javascript
// НЕ идемпотентный пример (инкремент)
PATCH /articles/123
Body: { views: views + 1 }  // Увеличить счётчик

// Первый запрос: views = 100 → 101
// Повторный запрос: views = 101 → 102 (другой результат!)

// ❌ Не идемпотентный
```

```javascript
// Идемпотентный пример (установка значения)
PATCH /articles/123
Body: { views: 101 }  // Установить конкретное значение

// Первый запрос: views = 101
// Повторный запрос: views = 101 (тот же результат!)

// ✅ Идемпотентный
```

### Зачем нужна идемпотентность?

#### 1. Безопасность при повторных запросах

Если запрос не дошёл или произошла ошибка, можно **безопасно повторить**:

```javascript
// Сетевой сбой после отправки PUT запроса
fetch('/api/users/123', {
  method: 'PUT',
  body: JSON.stringify({ name: 'Алексей' })
})
.catch(() => {
  // Можно безопасно повторить — результат будет тот же
  return fetch('/api/users/123', {
    method: 'PUT',
    body: JSON.stringify({ name: 'Алексей' })
  });
});
```

#### 2. Защита от дублирования

Идемпотентные запросы можно повторять без страха создать дубликаты:

```javascript
// ❌ POST — может создать дубликат
POST /orders  // Создаёт заказ #1
POST /orders  // Создаёт заказ #2 (дубликат!)

// ✅ PUT — можно повторять безопасно
PUT /orders/123  // Создаёт/обновляет заказ #123
PUT /orders/123  // То же самое (безопасно)
```

#### 3. Кеширование и оптимизация

Идемпотентные запросы можно кешировать и оптимизировать:

```javascript
// GET запросы можно кешировать
GET /users/123  // Кешируется браузером
GET /users/123  // Берётся из кеша (быстрее)
```

### Визуальная аналогия

**Идемпотентный запрос** — как **переключить выключатель в положение "вкл"**:
- Первый раз: включить свет
- Второй раз: свет уже включён (тот же результат)
- Третий раз: свет всё ещё включён (тот же результат)

**Неидемпотентный запрос** — как **добавить монету в копилку**:
- Первый раз: 1 монета
- Второй раз: 2 монеты (другой результат!)
- Третий раз: 3 монеты (снова другой результат!)

### ⚠️ Частая ошибка

Думают, что идемпотентность = безопасность:

```javascript
// ❌ Неправильное понимание
// Идемпотентный запрос = безопасный запрос

// ✅ Правильное понимание
// Идемпотентный — можно повторять без изменения результата
// Безопасный — не изменяет состояние (только GET)
```

**Разница:**
- **Безопасный** (safe) — не изменяет состояние сервера (только `GET`, `HEAD`, `OPTIONS`)
- **Идемпотентный** — можно повторять без изменения результата (`GET`, `PUT`, `DELETE`)

### Как сделать POST идемпотентным?

Иногда нужно сделать POST идемпотентным (например, для платежей):

#### Вариант 1: Идемпотентный ключ

```javascript
// Клиент генерирует уникальный ключ
const idempotencyKey = 'req-123-abc';

POST /orders
Headers: { 'Idempotency-Key': 'req-123-abc' }
Body: { product: 'laptop', price: 1000 }

// Сервер проверяет ключ
// Если такой ключ уже был — возвращает тот же заказ
// Если нет — создаёт новый
```

#### Вариант 2: Условный POST

```javascript
POST /orders
Headers: { 'If-None-Match': '*' }  // Создать только если не существует
Body: { product: 'laptop', price: 1000 }
```

### Итоги

- Идемпотентность — повторное выполнение даёт тот же результат
- Идемпотентные методы: `GET`, `PUT`, `DELETE`
- Неидемпотентные: `POST` (всегда), `PATCH` (зависит)
- Идемпотентность позволяет безопасно повторять запросы
- Идемпотентность ≠ безопасность (безопасный только GET)
- Можно сделать POST идемпотентным с помощью ключей

---

## 6. HTTP статус-коды (2xx, 3xx, 4xx, 5xx)

HTTP статус-коды — это **трёхзначные числа**, которые сервер отправляет в ответе, чтобы сообщить клиенту о результате обработки запроса.

### Простое определение

Статус-коды — это "ответы" сервера:
- **2xx** — "Всё хорошо" (успех)
- **3xx** — "Иди туда" (перенаправление)
- **4xx** — "Ты ошибся" (ошибка клиента)
- **5xx** — "Я ошибся" (ошибка сервера)

### Структура статус-кода

```
HTTP/1.1 200 OK
        ^^^
        |
     Статус-код
```

**Формат:** `XXX` — три цифры

- **Первая цифра** — категория (2, 3, 4, 5)
- **Вторые две цифры** — конкретный код

### 2xx — Успешные запросы

Сервер **успешно обработал** запрос.

#### 200 OK — Стандартный успешный ответ

```javascript
fetch('https://api.example.com/users/123')
  .then(response => {
    if (response.status === 200) {
      console.log('Успешно!');
    }
  });
```

**Когда используется:**
- GET запрос вернул данные
- PUT/PATCH успешно обновил ресурс
- DELETE успешно удалил ресурс

#### 201 Created — Ресурс создан

```javascript
fetch('https://api.example.com/users', {
  method: 'POST',
  body: JSON.stringify({ name: 'Алекс' })
})
  .then(response => {
    if (response.status === 201) {
      console.log('Пользователь создан!');
      // Обычно есть заголовок Location с адресом нового ресурса
      const location = response.headers.get('Location');
    }
  });
```

**Когда используется:**
- POST запрос успешно создал новый ресурс
- Обычно есть заголовок `Location: /users/123`

#### 204 No Content — Успешно, но без содержимого

```javascript
fetch('https://api.example.com/users/123', {
  method: 'DELETE'
})
  .then(response => {
    if (response.status === 204) {
      console.log('Удалено!');
      // response.body пустое
    }
  });
```

**Когда используется:**
- DELETE успешно удалил ресурс
- PUT/PATCH успешно обновил, но не нужно возвращать данные
- Тело ответа пустое

### 3xx — Перенаправления

Клиент должен выполнить **дополнительные действия** для завершения запроса.

#### 301 Moved Permanently — Постоянное перенаправление

```
HTTP/1.1 301 Moved Permanently
Location: https://www.example.com/new-page
```

**Когда используется:**
- Страница **навсегда** переехала на новый адрес
- Браузер должен обновить закладку
- Поисковики обновляют индекс

#### 302 Found — Временное перенаправление

```
HTTP/1.1 302 Found
Location: https://example.com/temporary-page
```

**Когда используется:**
- Страница **временно** переехала
- Браузер НЕ должен обновлять закладку
- При следующем запросе может вернуться на старый адрес

#### 304 Not Modified — Не изменено (кеш)

```javascript
// Браузер отправляет заголовок If-None-Match
GET /page.html
If-None-Match: "abc123"

// Сервер отвечает 304, если ресурс не изменился
HTTP/1.1 304 Not Modified
ETag: "abc123"
```

**Когда используется:**
- Ресурс не изменился с последнего запроса
- Браузер использует кешированную версию
- Экономит трафик

### 4xx — Ошибки клиента

Клиент **сделал что-то не так**.

#### 400 Bad Request — Неверный запрос

```javascript
fetch('https://api.example.com/users', {
  method: 'POST',
  body: JSON.stringify({ name: '' })  // Пустое имя
})
  .then(response => {
    if (response.status === 400) {
      console.log('Неправильный запрос!');
    }
  });
```

**Когда используется:**
- Невалидные данные в запросе
- Неправильный формат JSON
- Отсутствуют обязательные поля

#### 401 Unauthorized — Не авторизован

```javascript
fetch('https://api.example.com/profile', {
  headers: {
    // Нет токена авторизации
  }
})
  .then(response => {
    if (response.status === 401) {
      console.log('Нужна авторизация!');
      // Перенаправить на страницу входа
    }
  });
```

**Когда используется:**
- Требуется авторизация
- Токен отсутствует или неверный
- Нужно войти в систему

#### 403 Forbidden — Доступ запрещён

```javascript
fetch('https://api.example.com/admin/users', {
  headers: {
    'Authorization': 'Bearer valid-token'  // Токен есть, но нет прав
  }
})
  .then(response => {
    if (response.status === 403) {
      console.log('Нет прав доступа!');
    }
  });
```

**Когда используется:**
- Пользователь авторизован, но **нет прав** на ресурс
- Доступ запрещён по политике

#### 404 Not Found — Не найдено

```javascript
fetch('https://api.example.com/users/99999')  // Несуществующий пользователь
  .then(response => {
    if (response.status === 404) {
      console.log('Ресурс не найден!');
    }
  });
```

**Когда используется:**
- Ресурс не существует
- Неверный URL
- Самая частая ошибка в веб-разработке 😄

#### 409 Conflict — Конфликт

```javascript
fetch('https://api.example.com/users', {
  method: 'POST',
  body: JSON.stringify({ email: 'existing@example.com' })  // Email уже существует
})
  .then(response => {
    if (response.status === 409) {
      console.log('Конфликт! Email уже используется');
    }
  });
```

**Когда используется:**
- Конфликт при создании/обновлении ресурса
- Email уже существует
- Версия ресурса не совпадает

### 5xx — Ошибки сервера

Сервер **не смог обработать** запрос.

#### 500 Internal Server Error — Внутренняя ошибка сервера

```javascript
fetch('https://api.example.com/users')
  .then(response => {
    if (response.status === 500) {
      console.log('Ошибка на сервере!');
    }
  });
```

**Когда используется:**
- Необработанное исключение на сервере
- Ошибка в коде сервера
- Общая ошибка, когда не знаешь, что именно сломалось

#### 502 Bad Gateway — Неверный ответ от шлюза

```
Client → Load Balancer → Server
              ↑
         Ошибка здесь
```

**Когда используется:**
- Сервер-шлюз получил неверный ответ от upstream сервера
- Проблемы с балансировщиком нагрузки

#### 503 Service Unavailable — Сервис недоступен

```javascript
fetch('https://api.example.com/users')
  .then(response => {
    if (response.status === 503) {
      console.log('Сервис временно недоступен!');
      // Можно повторить запрос позже
    }
  });
```

**Когда используется:**
- Сервер временно перегружен
- Идёт техническое обслуживание
- Временно недоступен

#### 504 Gateway Timeout — Таймаут шлюза

**Когда используется:**
- Сервер-шлюз не получил ответ вовремя от upstream сервера
- Превышено время ожидания

### Быстрая справочная таблица

| Код | Категория | Значение | Примеры |
|-----|-----------|----------|---------|
| **200** | 2xx | OK | Успешный GET, PUT, PATCH |
| **201** | 2xx | Created | Ресурс создан (POST) |
| **204** | 2xx | No Content | Успешный DELETE |
| **301** | 3xx | Moved Permanently | Постоянное перенаправление |
| **302** | 3xx | Found | Временное перенаправление |
| **304** | 3xx | Not Modified | Использовать кеш |
| **400** | 4xx | Bad Request | Невалидные данные |
| **401** | 4xx | Unauthorized | Нужна авторизация |
| **403** | 4xx | Forbidden | Нет прав доступа |
| **404** | 4xx | Not Found | Ресурс не найден |
| **409** | 4xx | Conflict | Конфликт данных |
| **500** | 5xx | Internal Server Error | Ошибка на сервере |
| **502** | 5xx | Bad Gateway | Проблема со шлюзом |
| **503** | 5xx | Service Unavailable | Сервис недоступен |
| **504** | 5xx | Gateway Timeout | Таймаут шлюза |

### Визуальная аналогия

Статус-коды — как **ответы официанта в ресторане**:

- **2xx** — "Заказ выполнен!" (200 — готово, 201 — новое блюдо создано)
- **3xx** — "Идите за столом №5" (перенаправление)
- **4xx** — "Вы заказали то, чего нет в меню" (ваша ошибка)
- **5xx** — "Извините, на кухне проблема" (наша ошибка)

### ⚠️ Частая ошибка

Путают 401 и 403:

```javascript
// ❌ Неправильное понимание
// 401 = нет доступа
// 403 = нет доступа

// ✅ Правильное понимание
// 401 = не авторизован (нет токена/логина)
// 403 = авторизован, но нет прав
```

### Как обрабатывать статус-коды в коде

```javascript
async function fetchUser(id) {
  const response = await fetch(`/api/users/${id}`);
  
  if (response.ok) {  // 200-299
    return await response.json();
  }
  
  switch (response.status) {
    case 401:
      // Перенаправить на страницу входа
      window.location.href = '/login';
      break;
    case 403:
      // Показать сообщение "Нет доступа"
      throw new Error('Доступ запрещён');
    case 404:
      // Показать "Не найдено"
      throw new Error('Пользователь не найден');
    case 500:
      // Показать "Ошибка сервера"
      throw new Error('Ошибка на сервере');
    default:
      throw new Error('Неизвестная ошибка');
  }
}
```

### Итоги

- **2xx** — успешные запросы (200, 201, 204)
- **3xx** — перенаправления (301, 302, 304)
- **4xx** — ошибки клиента (400, 401, 403, 404, 409)
- **5xx** — ошибки сервера (500, 502, 503, 504)
- Первая цифра определяет категорию
- Важно правильно обрабатывать статус-коды в коде
- 401 = не авторизован, 403 = нет прав (важное отличие!)

---

## 7. Разница 401 vs 403

`401 Unauthorized` и `403 Forbidden` — оба указывают на проблемы с доступом, но означают **разные ситуации**.

### Простое определение

- **401 Unauthorized** — "Кто ты?" (не авторизован, нет удостоверения)
- **403 Forbidden** — "Ты не можешь этого делать" (авторизован, но нет прав)

### 401 Unauthorized — Не авторизован

Клиент **не предоставил** или предоставил **неверные** учётные данные.

#### Когда возникает

- ❌ Нет токена авторизации
- ❌ Токен истёк
- ❌ Неверный логин/пароль
- ❌ Токен недействителен

#### Примеры

**Нет токена:**
```javascript
fetch('https://api.example.com/profile')
  .then(response => {
    if (response.status === 401) {
      console.log('Нужно войти в систему');
      // Перенаправить на /login
    }
  });
```

**Неверный токен:**
```javascript
fetch('https://api.example.com/profile', {
  headers: {
    'Authorization': 'Bearer invalid-token'  // Токен недействителен
  }
})
  .then(response => {
    if (response.status === 401) {
      console.log('Токен истёк или неверный');
      // Обновить токен или перенаправить на /login
    }
  });
```

**Неверный логин/пароль:**
```javascript
fetch('https://api.example.com/login', {
  method: 'POST',
  body: JSON.stringify({
    username: 'alex',
    password: 'wrong-password'  // Неверный пароль
  })
})
  .then(response => {
    if (response.status === 401) {
      console.log('Неверный логин или пароль');
    }
  });
```

### 403 Forbidden — Доступ запрещён

Клиент **авторизован** (есть валидные учётные данные), но **не имеет прав** на доступ к ресурсу.

#### Когда возникает

- ✅ Пользователь авторизован
- ✅ Токен валидный
- ❌ Нет прав доступа к ресурсу
- ❌ Доступ запрещён по политике

#### Примеры

**Пользователь не админ:**
```javascript
fetch('https://api.example.com/admin/users', {
  headers: {
    'Authorization': 'Bearer valid-user-token'  // Обычный пользователь
  }
})
  .then(response => {
    if (response.status === 403) {
      console.log('Только администраторы могут просматривать пользователей');
    }
  });
```

**Доступ к чужому ресурсу:**
```javascript
// Пользователь пытается удалить чужой профиль
fetch('https://api.example.com/users/999', {
  method: 'DELETE',
  headers: {
    'Authorization': 'Bearer valid-token'  // Авторизован, но это не его профиль
  }
})
  .then(response => {
    if (response.status === 403) {
      console.log('Вы можете удалять только свой профиль');
    }
  });
```

**Ограничение по IP:**
```javascript
fetch('https://api.example.com/internal-data', {
  headers: {
    'Authorization': 'Bearer valid-token'  // Токен валидный
  }
  // Но IP адрес не в белом списке
})
  .then(response => {
    if (response.status === 403) {
      console.log('Доступ разрешён только с корпоративной сети');
    }
  });
```

### Сравнительная таблица

| Характеристика | 401 Unauthorized | 403 Forbidden |
|---------------|------------------|---------------|
| **Авторизован?** | ❌ Нет | ✅ Да |
| **Токен/логин** | ❌ Нет или неверный | ✅ Есть и валидный |
| **Проблема** | Нет удостоверения | Нет прав доступа |
| **Решение** | Войти/получить токен | Получить права (обычно нельзя) |
| **Аналогия** | "Покажи пропуск" | "У тебя нет доступа в эту зону" |

### Визуальная аналогия

**401 Unauthorized** — как **попытка войти в здание без пропуска**:
- Охранник: "У вас нет пропуска, войдите через главный вход"
- Решение: Получить пропуск (войти в систему)

**403 Forbidden** — как **попытка войти в закрытую зону с обычным пропуском**:
- У вас есть пропуск (авторизован)
- Но пропуск не даёт доступа в эту зону (нет прав)
- Решение: Получить пропуск более высокого уровня (обычно невозможно)

### Последовательность проверки на сервере

```javascript
// Псевдокод сервера
function handleRequest(req) {
  // Шаг 1: Проверка авторизации
  const token = req.headers.authorization;
  if (!token || !isValidToken(token)) {
    return 401;  // Нет токена или неверный
  }
  
  const user = getUserFromToken(token);
  
  // Шаг 2: Проверка прав
  if (!hasPermission(user, req.resource)) {
    return 403;  // Токен валидный, но нет прав
  }
  
  // Всё хорошо
  return 200;
}
```

**Важно:** Сначала проверяется **401** (авторизация), потом **403** (права).

### Практические примеры

#### Пример 1: API с защищёнными роутами

```javascript
// ❌ 401: Нет токена
fetch('/api/profile')
  .then(response => {
    if (response.status === 401) {
      // Перенаправить на страницу входа
      window.location.href = '/login';
    }
  });

// ✅ 401: Неверный токен
fetch('/api/profile', {
  headers: { 'Authorization': 'Bearer expired-token' }
})
  .then(response => {
    if (response.status === 401) {
      // Обновить токен или перенаправить на вход
      refreshToken();
    }
  });

// ✅ 403: Нет прав
fetch('/api/admin/users', {
  headers: { 'Authorization': 'Bearer valid-user-token' }
})
  .then(response => {
    if (response.status === 403) {
      // Показать сообщение "Нет доступа"
      showError('Только администраторы могут просматривать пользователей');
    }
  });
```

#### Пример 2: Обработка в React

```javascript
function useApi() {
  const [error, setError] = useState(null);
  
  async function fetchData(url) {
    try {
      const response = await fetch(url, {
        headers: {
          'Authorization': `Bearer ${getToken()}`
        }
      });
      
      if (response.status === 401) {
        // Токен истёк или неверный
        logout();
        navigate('/login');
        return;
      }
      
      if (response.status === 403) {
        // Нет прав доступа
        setError('У вас нет прав для выполнения этого действия');
        return;
      }
      
      if (!response.ok) {
        throw new Error('Ошибка запроса');
      }
      
      return await response.json();
    } catch (err) {
      setError(err.message);
    }
  }
  
  return { fetchData, error };
}
```

### ⚠️ Частая ошибка

Путают 401 и 403:

```javascript
// ❌ Неправильно
if (response.status === 401) {
  showError('У вас нет прав доступа');  // Это сообщение для 403!
}

// ✅ Правильно
if (response.status === 401) {
  showError('Необходима авторизация');
  navigate('/login');
}

if (response.status === 403) {
  showError('У вас нет прав доступа');
}
```

### Заголовок WWW-Authenticate

**401** может включать заголовок `WWW-Authenticate`, указывающий способ авторизации:

```
HTTP/1.1 401 Unauthorized
WWW-Authenticate: Bearer realm="example"
```

**403** обычно не включает этот заголовок, так как авторизация уже пройдена.

### Итоги

- **401 Unauthorized** — не авторизован (нет токена/логина или неверный)
- **403 Forbidden** — авторизован, но нет прав доступа
- **401** решается входом/получением токена
- **403** обычно не решается пользователем (нужны права от администратора)
- Сначала проверяется авторизация (401), потом права (403)
- Важно правильно различать эти коды для корректной обработки ошибок
