## JavaScript

Подборка вопросов и ответов по JavaScript в формате **вопрос–ответ**.

Материал сгруппирован по темам и предназначен для:

- подготовки к техническим собеседованиям
- повторения ключевых концепций языка
- систематизации знаний

---

## ⚙ Содержание

### Основы языка

▸ [docs/basics.md](./docs/basics.md)

- Типы данных в JavaScript
- Примитивные и ссылочные типы
- Truthy и falsy значения
- `typeof` и его особенности
- Разница между `null` и `undefined`
- Как проверить значение на `NaN`
- Разница между `isNaN` и `Number.isNaN`
- Неявное приведение типов (type coercion)
- Разница между `==` и `===`
- Странные примеры приведения типов
- Отличия `var`, `let` и `const`
- Temporal Dead Zone (TDZ)
- Block scope и function scope
- Strict mode в JavaScript
- Ключевые отличия ES5 и ES6+

---

### Область видимости и функции

▸ [docs/scope-functions.md](./docs/scope-functions.md)

- Что такое hoisting и что именно поднимается
- Что такое область видимости (scope)
- Что такое лексическое окружение
- Разница между `function declaration` и `function expression`
- Стрелочные функции и их особенности
- Есть ли у стрелочных функций `this`, `arguments`, `prototype`
- Что такое `this` и как он определяется
- Потеря контекста `this`
- `call`, `apply`, `bind` и разница между ними
- Что такое IIFE
- Как передаются аргументы в функции (по значению и по ссылке)
- Что такое замыкание
- Замыкания и приватные переменные
- Замыкания в циклах (`var` vs `let`)

---

### Объекты и массивы

▸ [docs/objects-arrays.md](./docs/objects-arrays.md)

- Изменяемы ли строки в JavaScript
- Что такое объект в JavaScript
- Как проверить, что значение — массив
- Как проверить наличие свойства в объекте
- Разница между `in` и `hasOwnProperty`
- Итерация по объекту
- Почему `{}` !== `{}` и как сравниваются объекты
- Что такое прототип
- `__proto__`, `[[Prototype]]`, `Object.getPrototypeOf`
- Цепочка прототипов
- Что находится в конце цепочки прототипов
- Поверхностное и глубокое копирование
- Способы копирования объектов
- Как сделать глубокую копию объекта
- Ограничения `JSON.parse(JSON.stringify())`
- Какие типы данных теряются при `JSON.stringify`
- Мутация и иммутабельность
- Методы массивов `map`, `filter`, `reduce`
- Разница между `map` и `forEach`
- Методы массива `find`, `some`, `every`
- Мутирующие и немутирующие методы массивов
- `Set`, `Map`, `WeakSet`, `WeakMap`
- Отличие `Map` от обычного объекта
- Зачем нужны `WeakMap` и `WeakSet`

---

### Асинхронность и Event Loop

▸ [docs/async-event-loop.md](./docs/async-event-loop.md)

- Что такое Promise
- Состояния Promise
- Цепочки `.then`
- Способы обработки ошибок в Promise
- `Promise.all`, `Promise.race`, `Promise.allSettled`, `Promise.any`
- Что происходит, если Promise в `Promise.all` отклоняется
- Что такое `async / await`
- Что возвращает `async` функция
- Можно ли использовать `await` вне `async`
- Что такое Event Loop и зачем он нужен
- Call Stack
- Microtasks и Macrotasks
- Порядок выполнения `async / await`
- Когда реально выполняется `setTimeout`
- Порядок выполнения `Promise`, `setTimeout`, `async / await`

---

### Ошибки и ES

▸ [docs/errors-es.md](./docs/errors-es.md)

- Как работает `try / catch / finally`
- Как ловить ошибки в `async / await`
- Почему `try/catch` не ловит ошибку в `setTimeout`

---

## Формат

- Каждый пункт — отдельный вопрос
- Краткий и точный ответ по существу
- Примеры кода — там, где это необходимо для понимания

---

## Примечание

Материал ориентирован на **понимание**, а не заучивание.  
Рекомендуется проверять примеры и экспериментировать с кодом самостоятельно.
