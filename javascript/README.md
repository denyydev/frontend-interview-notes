# JavaScript — вопросы и ответы

Этот репозиторий — конспект и справочник по JavaScript в формате **вопрос–ответ**.  
Подойдёт для:

- подготовки к собеседованиям
- повторения базовых и продвинутых тем
- быстрого освежения знаний

Все материалы разбиты на несколько крупных тематических файлов для удобной навигации.

---

## 📚 Содержание

### 🧩 Основы языка

👉 [docs/basics.md](./docs/basics.md)

- Типы данных в JavaScript
- Примитивные и ссылочные типы
- Truthy / Falsy значения
- `typeof` и его особенности
- `null` vs `undefined`
- `NaN`, `isNaN` и `Number.isNaN`
- Неявное приведение типов
- `==` vs `===`
- Странные примеры type coercion

---

### 🔍 Область видимости и функции

👉 [docs/scope-functions.md](./docs/scope-functions.md)

- `var`, `let`, `const`, TDZ
- Block scope и function scope
- Strict mode
- Hoisting
- Scope и лексическое окружение
- Function Declaration vs Function Expression
- Стрелочные функции и их особенности
- `this`, потеря контекста
- `call`, `apply`, `bind`
- IIFE
- Передача аргументов (value vs reference)
- Замыкания и приватные переменные
- Замыкания в циклах (`var` vs `let`)

---

### 🧱 Объекты и массивы

👉 [docs/objects-arrays.md](./docs/objects-arrays.md)

- Изменяемость строк
- Объекты и свойства
- Проверка массивов
- Проверка наличия свойства
- `in` vs `hasOwnProperty`
- Итерация по объекту
- Почему `{}` !== `{}`
- Прототипы и цепочка прототипов
- `__proto__`, `[[Prototype]]`, `Object.getPrototypeOf`
- Поверхностное и глубокое копирование
- Способы копирования объектов
- Ограничения `JSON.parse(JSON.stringify())`
- Потеря типов при `JSON.stringify`
- Мутация и иммутабельность
- Методы массивов (`map`, `filter`, `reduce`, и др.)
- `Map`, `Set`, `WeakMap`, `WeakSet`

---

### ⏱ Асинхронность и Event Loop

👉 [docs/async-event-loop.md](./docs/async-event-loop.md)

- `Promise` и его состояния
- Цепочки `.then`
- Обработка ошибок в Promise
- `Promise.all`, `race`, `allSettled`, `any`
- `async / await`
- Что возвращает `async` функция
- Можно ли использовать `await` вне `async`
- Event Loop
- Call Stack
- Microtasks и Macrotasks
- Порядок выполнения `Promise`, `setTimeout`, `async / await`
- Когда реально выполняется `setTimeout`

---

### 🚨 Ошибки и ES

👉 [docs/errors-es.md](./docs/errors-es.md)

- `try / catch / finally`
- Ловля ошибок в `async / await`
- Почему `try/catch` не ловит ошибку в `setTimeout`
- Ключевые отличия ES5 и ES6+

---

## 📝 Формат

- Каждый пункт — отдельный вопрос
- Краткий, но точный ответ
- Примеры кода там, где это необходимо

---

## 📌 Примечание

Материал ориентирован на **понимание**, а не заучивание.  
Рекомендуется экспериментировать с примерами в консоли или песочнице.

---

Удачи в изучении JavaScript 🚀
