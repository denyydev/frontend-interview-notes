# TypeScript — Основы

> Подробный конспект по основам TypeScript для подготовки к собеседованиям и изучения языка

---

## Содержание (Table of Contents)

### 📌 Основы TypeScript

1. [Что такое TypeScript и зачем он нужен](#1-что-такое-typescript-и-зачем-он-нужен)
2. [Отличия TypeScript от JavaScript](#2-отличия-typescript-от-javascript)
3. [Что компилирует TypeScript](#3-что-компилирует-typescript)
4. [Что такое `tsconfig.json`](#4-что-такое-tsconfigjson)
5. [Основные флаги `tsconfig` (`target`, `module`, `strict`, `lib`)](#5-основные-флаги-tsconfig-target-module-strict-lib)
6. [Что включает режим `strict`](#6-что-включает-режим-strict)

---

## 1. Что такое TypeScript и зачем он нужен

TypeScript — это **надмножество JavaScript**, которое добавляет статическую типизацию к языку. Разработан компанией Microsoft.

### Простыми словами

TypeScript — это **JavaScript с типами**. Ты пишешь почти такой же код, как в JavaScript, но можешь указывать типы переменных, функций и объектов.

### Что даёт TypeScript?

#### 1. Обнаружение ошибок до выполнения кода

**JavaScript:**
```js
function greet(name) {
  return `Привет, ${name}!`;
}

greet(42);  // Работает, но странно: "Привет, 42!"
greet();     // Ошибка только в runtime: "Привет, undefined!"
```

**TypeScript:**
```ts
function greet(name: string) {
  return `Привет, ${name}!`;
}

greet(42);  // ❌ Ошибка компиляции: Argument of type 'number' is not assignable to parameter of type 'string'
greet();     // ❌ Ошибка компиляции: Expected 1 arguments, but got 0
```

**Преимущество:** Ошибки видны **до запуска** программы, а не во время работы.

#### 2. Автодополнение и подсказки в IDE

```ts
interface User {
  name: string;
  age: number;
  email: string;
}

function getUser(): User {
  return {
    name: 'Алекс',
    age: 25,
    email: 'alex@example.com'
  };
}

const user = getUser();
user.  // IDE покажет: name, age, email
user.na  // IDE предложит: name
user.age.toFixed(  // IDE покажет параметры toFixed
```

**Без TypeScript:** IDE не знает, какие свойства есть у объекта.

#### 3. Рефакторинг становится безопаснее

```ts
// Меняем название свойства
interface User {
  // name: string;  ← удалили
  fullName: string;  // ← добавили новое
}

// TypeScript найдёт ВСЕ места, где используется name
// И покажет ошибки там, где нужно изменить код
```

**Без TypeScript:** Приходится искать вручную, легко что-то пропустить.

#### 4. Самодокументируемый код

```ts
// JavaScript — непонятно, что ожидается
function calculateTotal(items, discount) {
  // Что такое items? Массив? Объект?
  // Что такое discount? Число? Процент? Десятичная дробь?
  return items.reduce((sum, item) => sum + item.price, 0) * (1 - discount);
}

// TypeScript — сразу понятно
function calculateTotal(
  items: { price: number }[], 
  discount: number
): number {
  return items.reduce((sum, item) => sum + item.price, 0) * (1 - discount);
}
```

Типы **сами объясняют**, что ожидает функция.

### Пример проблемы, которую решает TypeScript

**JavaScript:**
```js
function processUser(user) {
  console.log(user.name.toUpperCase());
  console.log(user.age + 1);
}

// Где-то в коде передали не тот объект
processUser({ name: null, age: '25' });  
// Runtime ошибка: Cannot read property 'toUpperCase' of null
// Runtime ошибка: '25' + 1 = '251' (строковая конкатенация)
```

**TypeScript:**
```ts
interface User {
  name: string;
  age: number;
}

function processUser(user: User) {
  console.log(user.name.toUpperCase());
  console.log(user.age + 1);
}

// Ошибка компиляции — не скомпилируется
processUser({ name: null, age: '25' });  
// ❌ Type 'null' is not assignable to type 'string'
// ❌ Type 'string' is not assignable to type 'number'
```

### Визуальная аналогия

TypeScript — как **GPS-навигатор**:
- **JavaScript** — едешь без карты, надеешься на память (можешь заблудиться)
- **TypeScript** — GPS показывает дорогу и предупреждает о поворотах (помогает не заблудиться)

Или как **шлемы на стройке**:
- **JavaScript** — работаешь без шлема (можно и так, но рискованно)
- **TypeScript** — надеваешь шлем (защищает от ошибок)

### Зачем нужен TypeScript?

1. **Большие проекты** — легче поддерживать и понимать код
2. **Команды разработчиков** — типы — это способ коммуникации между разработчиками
3. **Меньше багов** — многие ошибки обнаруживаются на этапе компиляции
4. **Лучший DX (Developer Experience)** — автодополнение, подсказки, навигация по коду

### ⚠️ Частая ошибка

Думают, что TypeScript **заменяет** JavaScript:

```ts
// ❌ Неправильное понимание
// TypeScript — это не отдельный язык

// ✅ Правильно
// TypeScript — это JavaScript + типы
// Весь валидный JavaScript — это валидный TypeScript
```

**Важно:** TypeScript **компилируется** в JavaScript. В браузере и Node.js выполняется обычный JavaScript.

### Итоги

- TypeScript — надмножество JavaScript с типизацией
- Обнаруживает ошибки до выполнения кода
- Даёт автодополнение и подсказки в IDE
- Делает рефакторинг безопаснее
- Улучшает читаемость и самодокументируемость кода
- Всё равно компилируется в JavaScript

---

## 2. Отличия TypeScript от JavaScript

TypeScript и JavaScript — **очень похожие языки**, но есть ключевые отличия.

### Основные отличия

#### 1. Статическая типизация

**JavaScript — динамическая типизация:**
```js
let value = 42;
value = 'привет';     // ✅ Можно, переменная может менять тип
value = true;         // ✅ Можно
value = { name: 'Alex' }; // ✅ Можно

function add(a, b) {
  return a + b;
}

add(2, 3);        // ✅ 5
add('2', '3');    // ✅ '23' (не то, что хотелось)
add(2, '3');      // ✅ '23' (неожиданно)
add();            // ✅ NaN (но работает)
```

**TypeScript — статическая типизация:**
```ts
let value: number = 42;
value = 'привет';     // ❌ Type 'string' is not assignable to type 'number'
value = true;         // ❌ Type 'boolean' is not assignable to type 'number'
value = 100;          // ✅ Можно, тип совпадает

function add(a: number, b: number): number {
  return a + b;
}

add(2, 3);        // ✅ 5
add('2', '3');    // ❌ Type 'string' is not assignable to type 'number'
add(2, '3');      // ❌ Type 'string' is not assignable to type 'number'
add();            // ❌ Expected 2 arguments, but got 0
```

**Разница:** TypeScript проверяет типы **до выполнения** (compile-time), JavaScript — **во время выполнения** (runtime).

#### 2. Компиляция

**JavaScript:**
```js
// Файл: app.js
function greet(name) {
  return `Привет, ${name}!`;
}

greet('Алекс');
```

```bash
# Запускаем напрямую
node app.js
# Или в браузере
```

**TypeScript:**
```ts
// Файл: app.ts
function greet(name: string): string {
  return `Привет, ${name}!`;
}

greet('Алекс');
```

```bash
# Сначала компилируем
tsc app.ts
# Получаем app.js
# Затем запускаем
node app.js
```

**Разница:** TypeScript **компилируется** в JavaScript перед выполнением.

#### 3. Типы и интерфейсы

**JavaScript — нет типов:**
```js
// Нет способа описать структуру объекта
function processUser(user) {
  // Непонятно, что ожидается
  return user.name.toUpperCase();
}
```

**TypeScript — есть типы:**
```ts
interface User {
  name: string;
  age: number;
  email: string;
}

function processUser(user: User) {
  // Понятно, что ожидается
  return user.name.toUpperCase();
}

// Можно создать объект с типом
const user: User = {
  name: 'Алекс',
  age: 25,
  email: 'alex@example.com'
};
```

#### 4. Опциональные параметры и свойства

**JavaScript:**
```js
function greet(name, title) {
  if (title) {
    return `Привет, ${title} ${name}!`;
  }
  return `Привет, ${name}!`;
}

greet('Алекс');           // ✅ Работает
greet('Алекс', 'Mr.');    // ✅ Работает
greet();                  // ✅ Работает (но странно)
```

**TypeScript:**
```ts
function greet(name: string, title?: string): string {
  if (title) {
    return `Привет, ${title} ${name}!`;
  }
  return `Привет, ${name}!`;
}

greet('Алекс');           // ✅ Работает
greet('Алекс', 'Mr.');    // ✅ Работает
greet();                  // ❌ Expected 1-2 arguments, but got 0
```

**Разница:** TypeScript позволяет **явно** указать, что параметр опционален.

#### 5. Обработка ошибок

**JavaScript:**
```js
function divide(a, b) {
  return a / b;
}

divide(10, 0);    // ✅ Infinity (не ошибка, но странно)
divide(10, '2');  // ✅ 5 (работает, но неожиданно)
divide(10);       // ✅ NaN (работает, но неправильно)
```

**TypeScript:**
```ts
function divide(a: number, b: number): number {
  return a / b;
}

divide(10, 0);    // ✅ Infinity (но типы правильные)
divide(10, '2');  // ❌ Type 'string' is not assignable to type 'number'
divide(10);       // ❌ Expected 2 arguments, but got 1
```

### Сравнительная таблица

| Особенность | JavaScript | TypeScript |
|------------|-----------|------------|
| **Типизация** | Динамическая (runtime) | Статическая (compile-time) |
| **Компиляция** | Не нужна (интерпретируется) | Нужна (компилируется в JS) |
| **Типы** | Нет | Есть (типы, интерфейсы, generics) |
| **Проверка ошибок** | Во время выполнения | До выполнения |
| **Автодополнение** | Ограниченное | Полноценное |
| **Скорость разработки** | Быстрее (не нужно писать типы) | Медленнее (нужно писать типы) |
| **Надёжность** | Меньше (ошибки в runtime) | Больше (ошибки в compile-time) |

### Визуальная аналогия

**JavaScript** — как **свободное рисование**:
- Рисуешь что хочешь
- Никто не проверяет, пока не покажешь результат
- Может быть красиво, но могут быть ошибки

**TypeScript** — как **чертёж с правилами**:
- Есть правила и стандарты
- Проверяется до реализации
- Может быть медленнее, но надёжнее

### Что общего?

**Важно:** TypeScript — это **надмножество** JavaScript:
- Весь валидный JavaScript — это валидный TypeScript
- Все возможности JavaScript работают в TypeScript
- Синтаксис почти идентичен (кроме типов)

```ts
// Это валидный TypeScript
let x = 42;
const arr = [1, 2, 3];
function add(a, b) {
  return a + b;
}
// И это тоже
let x: number = 42;
const arr: number[] = [1, 2, 3];
function add(a: number, b: number): number {
  return a + b;
}
```

### ⚠️ Частая ошибка

Думают, что TypeScript **замедляет** разработку:

```ts
// ❌ Неправильно думать
// "TypeScript замедляет, нужно писать типы"

// ✅ Правильно
// TypeScript ускоряет на больших проектах
// Типы экономят время на отладку и рефакторинг
```

**Реальность:** На маленьких проектах может быть медленнее, но на больших — **быстрее** за счёт раннего обнаружения ошибок.

### Итоги

- TypeScript добавляет статическую типизацию к JavaScript
- Проверяет ошибки до выполнения кода (compile-time)
- Нужна компиляция в JavaScript
- Даёт автодополнение и лучший DX
- Весь JavaScript — валидный TypeScript
- На больших проектах экономит время

---

## 3. Что компилирует TypeScript

TypeScript **компилирует** (транспилирует) TypeScript-код в JavaScript-код, который может выполнять браузер или Node.js.

### Процесс компиляции

```
TypeScript файл (.ts)  →  [Компилятор TypeScript]  →  JavaScript файл (.js)
```

### Что происходит при компиляции?

#### 1. Удаление типов

**TypeScript (исходный код):**
```ts
function greet(name: string): string {
  return `Привет, ${name}!`;
}

const user: { name: string; age: number } = {
  name: 'Алекс',
  age: 25
};

interface User {
  name: string;
  age: number;
}
```

**JavaScript (скомпилированный код):**
```js
function greet(name) {
  return `Привет, ${name}!`;
}

const user = {
  name: 'Алекс',
  age: 25
};

// interface User — полностью удалён, его нет в JavaScript
```

**Важно:** Все типы, интерфейсы, type aliases **полностью удаляются** из JavaScript. Они существуют **только во время компиляции**.

#### 2. Преобразование синтаксиса

TypeScript может преобразовывать современный синтаксис в старый:

**TypeScript (с ES6+):**
```ts
const greet = (name: string): string => `Привет, ${name}!`;

const user = { name: 'Алекс', age: 25 };
const { name, age } = user;

class User {
  constructor(public name: string) {}
}
```

**JavaScript (ES5, если target: "ES5"):**
```js
var greet = function (name) {
  return "Привет, " + name + "!";
};

var user = { name: 'Алекс', age: 25 };
var name = user.name;
var age = user.age;

var User = (function () {
  function User(name) {
    this.name = name;
  }
  return User;
}());
```

**Разница:** Современный синтаксис преобразуется в старый для совместимости со старыми браузерами.

#### 3. Проверка типов

**До компиляции:**
```ts
function add(a: number, b: number): number {
  return a + b;
}

add('2', '3');  // ❌ Ошибка компиляции
```

**Результат:** Код **не скомпилируется**, JavaScript файл не будет создан.

**После исправления:**
```ts
function add(a: number, b: number): number {
  return a + b;
}

add(2, 3);  // ✅ Ошибок нет, компиляция успешна
```

**Результат:** Создаётся JavaScript файл.

### Что НЕ компилируется?

#### 1. Типы и интерфейсы (удаляются)

```ts
// TypeScript
type UserId = string;
interface User {
  name: string;
  age: number;
}

// JavaScript — ничего нет, типы удалены
```

#### 2. Enum (преобразуются, но остаются в runtime)

```ts
// TypeScript
enum Status {
  Pending,
  Completed,
  Failed
}

// JavaScript (если не использовать const enum)
var Status;
(function (Status) {
  Status[Status["Pending"] = 0] = "Pending";
  Status[Status["Completed"] = 1] = "Completed";
  Status[Status["Failed"] = 2] = "Failed";
})(Status || (Status = {}));
```

**⚠️ Важно:** Enum **остаются** в JavaScript (в отличие от типов).

#### 3. Декораторы (зависит от настроек)

```ts
// TypeScript
@Decorator
class User {}

// JavaScript (если experimentalDecorators: true)
var User = (function () {
  // ...
})();
__decorate([Decorator], User);
```

### Пример полной компиляции

**Исходный файл (`app.ts`):**
```ts
interface Person {
  name: string;
  age: number;
}

function greet(person: Person): string {
  return `Привет, ${person.name}! Тебе ${person.age} лет.`;
}

const user: Person = {
  name: 'Алекс',
  age: 25
};

console.log(greet(user));
```

**Скомпилированный файл (`app.js`):**
```js
function greet(person) {
  return "Привет, " + person.name + "! Тебе " + person.age + " лет.";
}

var user = {
  name: 'Алекс',
  age: 25
};

console.log(greet(user));
```

**Что изменилось:**
- ✅ `interface Person` — удалён
- ✅ Типы `: Person`, `: string` — удалены
- ✅ `const` остался (если target поддерживает const)
- ✅ Логика функции сохранена
- ✅ Шаблонные строки преобразованы (если target не поддерживает)

### Настройки компиляции (`tsconfig.json`)

Компилятор TypeScript можно настраивать:

```json
{
  "compilerOptions": {
    "target": "ES2020",      // Во что компилировать (версия JS)
    "module": "ES2020",      // Система модулей
    "outDir": "./dist",      // Куда складывать скомпилированные файлы
    "removeComments": true,  // Удалять комментарии
    "strict": true          // Строгая проверка типов
  }
}
```

### Визуальная аналогия

Компиляция TypeScript — как **перевод книги**:
- **TypeScript** — оригинальная книга (с типами)
- **Компилятор** — переводчик
- **JavaScript** — переведённая книга (без типов, но с той же логикой)

Типы — это **заметки переводчика**, которые не попадают в финальную книгу.

### ⚠️ Частая ошибка

Думают, что типы сохраняются в JavaScript:

```ts
// ❌ Неправильное понимание
// "Типы попадают в JavaScript код"

// ✅ Правильно
// Типы полностью удаляются при компиляции
// В JavaScript их нет
```

**Проверка:**
```ts
// app.ts
let x: number = 42;
```

```js
// app.js (после компиляции)
var x = 42;  // Типа нет!
```

### Итоги

- TypeScript компилируется в JavaScript
- Типы и интерфейсы полностью удаляются
- Синтаксис может преобразовываться для совместимости
- Проверка типов происходит до компиляции
- Enum остаются в JavaScript (если не const enum)
- Настройки компиляции задаются в `tsconfig.json`

---

## 4. Что такое `tsconfig.json`

`tsconfig.json` — это **файл конфигурации** компилятора TypeScript, который определяет, как компилировать TypeScript-проект.

### Зачем нужен `tsconfig.json`?

Без `tsconfig.json` компилятор использует значения по умолчанию. С ним можно настроить:
- Какой JavaScript генерировать
- Как проверять типы
- Какие файлы включать/исключать
- И многое другое

### Базовая структура

```json
{
  "compilerOptions": {
    // Опции компилятора
  },
  "include": [
    // Какие файлы компилировать
  ],
  "exclude": [
    // Какие файлы исключить
  ]
}
```

### Основные разделы

#### 1. `compilerOptions` — опции компилятора

```json
{
  "compilerOptions": {
    "target": "ES2020",           // Версия JavaScript для компиляции
    "module": "ES2020",           // Система модулей
    "lib": ["ES2020", "DOM"],     // Библиотеки типов
    "outDir": "./dist",           // Папка для скомпилированных файлов
    "rootDir": "./src",           // Корневая папка исходников
    "strict": true,               // Строгая проверка типов
    "esModuleInterop": true,      // Совместимость с CommonJS
    "skipLibCheck": true          // Пропускать проверку файлов .d.ts
  }
}
```

#### 2. `include` — какие файлы включить

```json
{
  "include": [
    "src/**/*"           // Все файлы в папке src
  ]
}
```

**Примеры:**
- `"src/**/*"` — все файлы в `src` и подпапках
- `["src/**/*.ts", "src/**/*.tsx"]` — только `.ts` и `.tsx` файлы
- `["*.ts"]` — все `.ts` файлы в корне

#### 3. `exclude` — какие файлы исключить

```json
{
  "exclude": [
    "node_modules",      // Исключить node_modules
    "dist",              // Исключить папку со скомпилированными файлами
    "**/*.test.ts"       // Исключить тестовые файлы
  ]
}
```

### Пример полного `tsconfig.json`

```json
{
  "compilerOptions": {
    // Целевая версия JavaScript
    "target": "ES2020",
    
    // Система модулей
    "module": "ES2020",
    
    // Библиотеки типов
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    
    // Папка для скомпилированных файлов
    "outDir": "./dist",
    
    // Корневая папка исходников
    "rootDir": "./src",
    
    // Строгая проверка типов
    "strict": true,
    
    // Дополнительные проверки
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noImplicitReturns": true,
    "noFallthroughCasesInSwitch": true,
    
    // Совместимость с модулями
    "esModuleInterop": true,
    "allowSyntheticDefaultImports": true,
    
    // Пропускать проверку библиотек
    "skipLibCheck": true,
    
    // Разрешить импорт JSON
    "resolveJsonModule": true,
    
    // Изоляция модулей
    "isolatedModules": true
  },
  
  "include": [
    "src/**/*"
  ],
  
  "exclude": [
    "node_modules",
    "dist",
    "**/*.test.ts",
    "**/*.spec.ts"
  ]
}
```

### Расширение конфигурации

Можно расширять другую конфигурацию:

```json
{
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "outDir": "./dist"
  }
}
```

**Полезно для:**
- Общих настроек для нескольких проектов
- Базовой конфигурации, которую расширяют

### Где размещать `tsconfig.json`?

`tsconfig.json` должен быть в **корне проекта**:

```
my-project/
├── tsconfig.json      ← Здесь
├── package.json
├── src/
│   ├── index.ts
│   └── app.ts
└── dist/
```

### Как компилятор находит `tsconfig.json`?

1. Ищет `tsconfig.json` в текущей папке
2. Если не найден, поднимается на уровень выше
3. И так до корня файловой системы
4. Если не найден, используются значения по умолчанию

### Команда компиляции

```bash
# Компилирует проект, используя tsconfig.json
tsc

# Или указать файл конфигурации явно
tsc --project tsconfig.json

# Или другой файл конфигурации
tsc --project tsconfig.prod.json
```

### Визуальная аналогия

`tsconfig.json` — как **инструкция для повара**:
- **Повар** (компилятор TypeScript)
- **Рецепт** (`tsconfig.json`)
- **Блюдо** (скомпилированный JavaScript)

Без инструкции повар готовит как умеет (значения по умолчанию).
С инструкцией готовит точно так, как нужно.

### ⚠️ Частая ошибка

Думают, что `tsconfig.json` обязателен:

```bash
# ❌ Неправильно думать
# "Без tsconfig.json проект не скомпилируется"

# ✅ Правильно
# tsconfig.json необязателен
# Можно использовать значения по умолчанию
# Или передать опции через командную строку: tsc --target ES2020
```

**Но:** На практике `tsconfig.json` **почти всегда используется** для настройки проекта.

### Итоги

- `tsconfig.json` — файл конфигурации компилятора TypeScript
- Определяет, как компилировать проект
- Содержит `compilerOptions`, `include`, `exclude`
- Размещается в корне проекта
- Необязателен, но практически всегда используется
- Можно расширять другую конфигурацию через `extends`

---

## 5. Основные флаги `tsconfig` (`target`, `module`, `strict`, `lib`)

В `tsconfig.json` есть много опций. Рассмотрим **основные и самые важные**.

### 1. `target` — целевая версия JavaScript

Определяет, **в какую версию JavaScript** компилировать TypeScript код.

```json
{
  "compilerOptions": {
    "target": "ES2020"
  }
}
```

**Пример:**

**TypeScript:**
```ts
const greet = (name: string) => `Привет, ${name}!`;
const user = { name: 'Алекс', age: 25 };
const { name, age } = user;
class User {
  constructor(public name: string) {}
}
```

**С `target: "ES2020"`:**
```js
const greet = (name) => `Привет, ${name}!`;
const user = { name: 'Алекс', age: 25 };
const { name, age } = user;
class User {
  constructor(name) {
    this.name = name;
  }
}
// Современный синтаксис сохраняется
```

**С `target: "ES5"`:**
```js
var greet = function (name) {
  return "Привет, " + name + "!";
};
var user = { name: 'Алекс', age: 25 };
var name = user.name;
var age = user.age;
var User = (function () {
  function User(name) {
    this.name = name;
  }
  return User;
}());
// Синтаксис преобразован в ES5
```

**Доступные значения:**
- `ES3`, `ES5`, `ES2015` (ES6), `ES2016`, `ES2017`, `ES2018`, `ES2019`, `ES2020`, `ES2021`, `ES2022`, `ESNext`

**Когда что использовать:**
- `ES5` — для поддержки старых браузеров (IE11)
- `ES2020` или новее — для современных проектов
- `ESNext` — использовать самые новые возможности

### 2. `module` — система модулей

Определяет, **какую систему модулей** использовать.

```json
{
  "compilerOptions": {
    "module": "ES2020"
  }
}
```

**Пример:**

**TypeScript:**
```ts
// math.ts
export function add(a: number, b: number): number {
  return a + b;
}

// app.ts
import { add } from './math';
```

**С `module: "ES2020"` или `"ESNext"`:**
```js
// math.js
export function add(a, b) {
  return a + b;
}

// app.js
import { add } from './math';
// ES modules (современные)
```

**С `module: "CommonJS"`:**
```js
// math.js
function add(a, b) {
  return a + b;
}
exports.add = add;

// app.js
const { add } = require('./math');
// CommonJS (Node.js стиль)
```

**С `module: "AMD"`:**
```js
// AMD модули (для браузеров, редкий случай)
define(["require", "exports"], function (require, exports) {
  exports.add = function (a, b) {
    return a + b;
  };
});
```

**Доступные значения:**
- `None`, `CommonJS`, `AMD`, `System`, `UMD`, `ES6` / `ES2015`, `ES2020`, `ESNext`

**Когда что использовать:**
- `ES2020` / `ESNext` — для современных браузеров
- `CommonJS` — для Node.js проектов
- `UMD` — для библиотек, которые должны работать везде

### 3. `strict` — строгая проверка типов

**Самый важный флаг!** Включает все строгие проверки типов.

```json
{
  "compilerOptions": {
    "strict": true
  }
}
```

Когда `strict: true`, включаются:
- `strictNullChecks` — строгая проверка `null` и `undefined`
- `strictFunctionTypes` — строгая проверка типов функций
- `strictBindCallApply` — строгая проверка `bind`, `call`, `apply`
- `strictPropertyInitialization` — проверка инициализации свойств класса
- `noImplicitAny` — ошибка при неявном `any`
- `noImplicitThis` — ошибка при неявном `this`

**Пример:**

**Без `strict`:**
```ts
function greet(name) {  // name: any (неявно)
  return `Привет, ${name}!`;
}

let user: { name: string };
console.log(user.name);  // ❌ Runtime ошибка, но компилируется
```

**С `strict: true`:**
```ts
function greet(name) {  // ❌ Parameter 'name' implicitly has an 'any' type
  return `Привет, ${name}!`;
}

let user: { name: string };
console.log(user.name);  // ❌ Variable 'user' is used before being assigned
```

**Рекомендация:** Всегда используй `strict: true` для новых проектов!

### 4. `lib` — библиотеки типов

Определяет, **какие библиотеки типов** доступны (например, типы для DOM API, встроенных объектов JavaScript).

```json
{
  "compilerOptions": {
    "lib": ["ES2020", "DOM", "DOM.Iterable"]
  }
}
```

**Пример:**

**С `lib: ["ES2020", "DOM"]`:**
```ts
// Доступны типы для:
console.log('hello');        // console (DOM)
document.getElementById('id'); // document (DOM)
[1, 2, 3].flat();            // Array.flat() (ES2020)
Promise.allSettled([]);      // Promise.allSettled() (ES2020)
```

**С `lib: ["ES5"]`:**
```ts
// ❌ Ошибки:
[1, 2, 3].flat();            // Property 'flat' does not exist on type 'number[]'
Promise.allSettled([]);      // Property 'allSettled' does not exist on type 'PromiseConstructor'
// console, document — доступны, если включен "DOM"
```

**Доступные значения:**
- `ES5`, `ES2015`, `ES2016`, `ES2017`, `ES2018`, `ES2019`, `ES2020`, `ES2021`, `ES2022`, `ESNext`
- `DOM` — типы для браузерного DOM API
- `DOM.Iterable` — типы для итерируемых DOM коллекций
- `WebWorker` — типы для Web Workers
- `ScriptHost` — типы для Windows Script Host

**Когда что использовать:**
- `["ES2020", "DOM"]` — для браузерных проектов
- `["ES2020"]` — для Node.js проектов (без DOM)
- `["ES5", "DOM"]` — для проектов, поддерживающих старые браузеры

### Пример полной конфигурации

```json
{
  "compilerOptions": {
    "target": "ES2020",              // Компилировать в ES2020
    "module": "ES2020",              // Использовать ES modules
    "lib": ["ES2020", "DOM"],        // Доступные библиотеки типов
    "strict": true,                  // Включить все строгие проверки
    "outDir": "./dist",              // Папка для скомпилированных файлов
    "rootDir": "./src",              // Корневая папка исходников
    "esModuleInterop": true,         // Совместимость с CommonJS
    "skipLibCheck": true             // Пропускать проверку .d.ts файлов
  }
}
```

### Визуальная аналогия

Эти флаги — как **настройки автомобиля**:
- **`target`** — какой тип топлива (бензин, дизель) — в какой JavaScript компилировать
- **`module`** — тип трансмиссии (механика, автомат) — какая система модулей
- **`strict`** — система безопасности (ABS, ESP) — строгие проверки
- **`lib`** — доступное оборудование (кондиционер, навигатор) — доступные API

### ⚠️ Частая ошибка

Не включают `strict` или не указывают `lib`:

```json
// ❌ Плохо
{
  "compilerOptions": {
    "target": "ES2020"
    // Нет strict: true
    // Нет lib
  }
}

// ✅ Хорошо
{
  "compilerOptions": {
    "target": "ES2020",
    "strict": true,              // Всегда включай!
    "lib": ["ES2020", "DOM"]     // Указывай явно
  }
}
```

**Рекомендация:** Всегда включай `strict: true` для новых проектов.

### Итоги

- `target` — версия JavaScript для компиляции (ES5, ES2020, и т.д.)
- `module` — система модулей (CommonJS, ES2020, и т.д.)
- `strict` — включает все строгие проверки типов (всегда используй!)
- `lib` — доступные библиотеки типов (ES2020, DOM, и т.д.)
- Эти флаги определяют, как компилируется и проверяется код

---

## 6. Что включает режим `strict`

Когда в `tsconfig.json` указано `"strict": true`, включаются **все строгие проверки типов** TypeScript. Это набор из нескольких опций, которые можно включать и по отдельности.

### Что включает `strict: true`?

```json
{
  "compilerOptions": {
    "strict": true
  }
}
```

Это **эквивалентно**:

```json
{
  "compilerOptions": {
    "strictNullChecks": true,
    "strictFunctionTypes": true,
    "strictBindCallApply": true,
    "strictPropertyInitialization": true,
    "noImplicitAny": true,
    "noImplicitThis": true
  }
}
```

Рассмотрим каждую опцию:

### 1. `strictNullChecks` — строгая проверка `null` и `undefined`

**Без `strictNullChecks`:**
```ts
let name: string;
name = null;        // ✅ Разрешено
name = undefined;   // ✅ Разрешено

function greet(name: string): string {
  return name.toUpperCase();  // Может быть null/undefined!
}

greet(null);  // ❌ Runtime ошибка, но компилируется
```

**С `strictNullChecks: true`:**
```ts
let name: string;
name = null;        // ❌ Type 'null' is not assignable to type 'string'
name = undefined;   // ❌ Type 'undefined' is not assignable to type 'string'

// Нужно явно указать
let name: string | null = null;  // ✅ Разрешено

function greet(name: string): string {
  return name.toUpperCase();  // name не может быть null/undefined
}

greet(null);  // ❌ Argument of type 'null' is not assignable to parameter of type 'string'
```

**Пример с проверкой:**
```ts
function greet(name: string | null): string {
  if (name === null) {
    return 'Привет, незнакомец!';
  }
  return `Привет, ${name}!`;  // ✅ TypeScript знает, что name не null
}
```

### 2. `noImplicitAny` — ошибка при неявном `any`

**Без `noImplicitAny`:**
```ts
function greet(name) {  // name: any (неявно)
  return `Привет, ${name}!`;
}

greet(42);     // ✅ Работает (хотя странно)
greet(true);   // ✅ Работает (хотя странно)
```

**С `noImplicitAny: true`:**
```ts
function greet(name) {  // ❌ Parameter 'name' implicitly has an 'any' type
  return `Привет, ${name}!`;
}

// Нужно явно указать тип
function greet(name: string) {  // ✅ Правильно
  return `Привет, ${name}!`;
}
```

**Явный `any` разрешён:**
```ts
function process(data: any) {  // ✅ Явный any разрешён
  // ...
}
```

### 3. `strictFunctionTypes` — строгая проверка типов функций

**Без `strictFunctionTypes`:**
```ts
type Handler = (event: MouseEvent) => void;

const onClick: Handler = (event: Event) => {  // ✅ Разрешено (но небезопасно)
  // event может быть не MouseEvent!
};

onClick(new KeyboardEvent('keydown'));  // ❌ Runtime ошибка
```

**С `strictFunctionTypes: true`:**
```ts
type Handler = (event: MouseEvent) => void;

const onClick: Handler = (event: Event) => {  
  // ❌ Type '(event: Event) => void' is not assignable to type 'Handler'
};
```

### 4. `strictBindCallApply` — строгая проверка `bind`, `call`, `apply`

**Без `strictBindCallApply`:**
```ts
function add(a: number, b: number): number {
  return a + b;
}

add.call(null, '2', '3');  // ✅ Компилируется (но вернёт '23')
add.apply(null, ['2', '3']);  // ✅ Компилируется (но вернёт '23')
```

**С `strictBindCallApply: true`:**
```ts
function add(a: number, b: number): number {
  return a + b;
}

add.call(null, '2', '3');  // ❌ Argument of type 'string' is not assignable to parameter of type 'number'
add.apply(null, ['2', '3']);  // ❌ Argument of type 'string' is not assignable to parameter of type 'number'
```

### 5. `strictPropertyInitialization` — проверка инициализации свойств класса

**Без `strictPropertyInitialization`:**
```ts
class User {
  name: string;  // Может быть не инициализировано
  
  constructor() {
    // Не инициализируем name
  }
  
  greet() {
    return `Привет, ${this.name}!`;  // ❌ Runtime ошибка, но компилируется
  }
}
```

**С `strictPropertyInitialization: true`:**
```ts
class User {
  name: string;  // ❌ Property 'name' has no initializer and is not definitely assigned in the constructor
  
  constructor() {
    // Не инициализируем name
  }
}

// Решения:
class User {
  name: string;
  
  constructor() {
    this.name = 'Незнакомец';  // ✅ Инициализируем в конструкторе
  }
}

// Или:
class User {
  name!: string;  // ✅ Definite assignment assertion (ты знаешь, что инициализируешь позже)
  
  constructor() {
    // Инициализация будет позже
  }
  
  init(name: string) {
    this.name = name;
  }
}

// Или:
class User {
  name?: string;  // ✅ Опциональное свойство
}
```

### 6. `noImplicitThis` — ошибка при неявном `this`

**Без `noImplicitThis`:**
```ts
class User {
  name: string = 'Алекс';
  
  greet() {
    return function() {
      return `Привет, ${this.name}!`;  // this может быть неправильным
    };
  }
}

const user = new User();
const greet = user.greet();
greet();  // ❌ Runtime ошибка: Cannot read property 'name' of undefined
```

**С `noImplicitThis: true`:**
```ts
class User {
  name: string = 'Алекс';
  
  greet() {
    return function() {
      return `Привет, ${this.name}!`;  
      // ❌ 'this' implicitly has type 'any' because it does not have a type annotation
    };
  }
}

// Решение:
class User {
  name: string = 'Алекс';
  
  greet() {
    return () => {  // ✅ Стрелочная функция сохраняет this
      return `Привет, ${this.name}!`;
    };
  }
}

// Или:
class User {
  name: string = 'Алекс';
  
  greet() {
    return function(this: User) {  // ✅ Явная типизация this
      return `Привет, ${this.name}!`;
    };
  }
}
```

### Сравнительная таблица

| Опция | Что проверяет | Без strict | С strict |
|------|---------------|------------|----------|
| `strictNullChecks` | `null` и `undefined` | Разрешены везде | Нужно явно указывать |
| `noImplicitAny` | Неявный `any` | `any` по умолчанию | Ошибка |
| `strictFunctionTypes` | Типы функций | Более мягкая проверка | Строгая проверка |
| `strictBindCallApply` | `bind`, `call`, `apply` | Может пропустить ошибки | Строгая проверка |
| `strictPropertyInitialization` | Инициализация свойств | Может быть не инициализировано | Должно быть инициализировано |
| `noImplicitThis` | Неявный `this` | `this` может быть `any` | Ошибка |

### Рекомендации

**✅ Всегда включай `strict: true` для новых проектов:**
- Обнаруживает больше ошибок до выполнения
- Делает код надёжнее
- Лучшая практика в TypeScript

**⚠️ Для старых проектов:**
- Можно включать постепенно
- Можно включать отдельные опции
- Но цель — включить всё

### Визуальная аналогия

`strict: true` — как **строгий учитель**:
- **Без strict** — учитель пропускает ошибки ("ладно, пойдёт")
- **С strict** — учитель указывает на все ошибки ("нет, исправь!")

Строгий учитель помогает не делать ошибок, но требует больше внимания.

### ⚠️ Частая ошибка

Отключают `strict` из-за ошибок:

```json
// ❌ Плохо
{
  "compilerOptions": {
    "strict": false  // Отключили из-за ошибок
  }
}
```

**Правильный подход:**
```json
// ✅ Хорошо
{
  "compilerOptions": {
    "strict": true  // Оставляем включенным
  }
}
// И исправляем ошибки, а не отключаем проверки
```

**Или включаем постепенно:**
```json
{
  "compilerOptions": {
    "strictNullChecks": true,  // Начни с этого
    "noImplicitAny": true      // Потом это
    // И так далее
  }
}
```

### Итоги

- `strict: true` включает 6 строгих проверок:
  1. `strictNullChecks` — строгая проверка null/undefined
  2. `noImplicitAny` — ошибка при неявном any
  3. `strictFunctionTypes` — строгая проверка типов функций
  4. `strictBindCallApply` — строгая проверка bind/call/apply
  5. `strictPropertyInitialization` — проверка инициализации свойств
  6. `noImplicitThis` — ошибка при неявном this
- Всегда используй `strict: true` для новых проектов
- Обнаруживает больше ошибок до выполнения кода
- Делает код надёжнее и предсказуемее

---