14. `enum` vs union of string literals — плюсы, минусы, влияние на бандл.
15. Что такое type assertion (`as`)? Когда его можно и нельзя использовать?
16. TypeScript + React: типизация `props`, `children`, `useState`, `useRef`, событий.
