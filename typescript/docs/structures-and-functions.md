10. Как типизировать функции в TypeScript? Optional и rest параметры.
11. Как типизировать объекты с динамическими ключами?  
    `index signature` vs `Record`.
12. Типизация массивов и кортежей (`Array<T>` vs `T[]`, tuple).
13. Что делает `readonly`? Почему `readonly` ≠ immutable?
