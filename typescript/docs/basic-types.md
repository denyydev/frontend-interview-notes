# TypeScript — Базовые типы

> Подробный конспект по базовым типам TypeScript для подготовки к собеседованиям и изучения языка

---

## Содержание (Table of Contents)

### 📌 Базовые типы

1. [Базовые типы в TypeScript](#1-базовые-типы-в-typescript)
2. [Разница между `any`, `unknown`, `never`](#2-разница-между-any-unknown-never)
3. [Почему `any` — плохо](#3-почему-any--плохо)
4. [Когда использовать `unknown`](#4-когда-использовать-unknown)
5. [Тип `void`](#5-тип-void)
6. [Типы `null` и `undefined`](#6-типы-null-и-undefined)
7. [Что меняет `strictNullChecks`](#7-что-меняет-strictnullchecks)

---

## 1. Базовые типы в TypeScript

TypeScript предоставляет набор **базовых типов**, которые используются для типизации переменных, функций и объектов.

### Примитивные типы

#### 1. `number` — числа

```ts
let age: number = 25;
let price: number = 99.99;
let count: number = -10;
let hex: number = 0xff;  // Шестнадцатеричные
let binary: number = 0b1010;  // Двоичные
let octal: number = 0o744;  // Восьмеричные
```

**Включает:**
- Целые числа (`42`)
- Дробные числа (`3.14`)
- Отрицательные числа (`-10`)
- `Infinity`, `-Infinity`
- `NaN` (хотя технически это `number`)

#### 2. `string` — строки

```ts
let name: string = 'Алекс';
let message: string = "Привет";
let template: string = `Привет, ${name}!`;  // Шаблонные строки
```

**Включает:**
- Одинарные кавычки (`'текст'`)
- Двойные кавычки (`"текст"`)
- Шаблонные строки (`` `текст` ``)

#### 3. `boolean` — логические значения

```ts
let isActive: boolean = true;
let isCompleted: boolean = false;
```

**Включает:**
- `true`
- `false`

#### 4. `null` и `undefined`

```ts
let nothing: null = null;
let notDefined: undefined = undefined;
```

**Важно:** По умолчанию `null` и `undefined` присваиваются любому типу (если не включён `strictNullChecks`).

### Специальные типы

#### 5. `any` — любой тип (избегай!)

```ts
let value: any = 42;
value = 'привет';      // ✅ Можно
value = true;          // ✅ Можно
value = {};            // ✅ Можно
value.foo.bar.baz;     // ✅ Не проверяется (опасно!)
```

**Проблема:** Отключает проверку типов для этой переменной.

#### 6. `unknown` — неизвестный тип (безопасная альтернатива `any`)

```ts
let value: unknown = 42;
value = 'привет';      // ✅ Можно
value = true;          // ✅ Можно

value.foo;  // ❌ Ошибка: Object is of type 'unknown'
```

**Разница с `any`:** Нельзя использовать без проверки типа.

#### 7. `void` — отсутствие значения

```ts
function logMessage(): void {
  console.log('Привет');
  // Ничего не возвращает
}

let result: void = undefined;  // void можно присвоить только undefined
```

**Используется для:** Функций, которые ничего не возвращают.

#### 8. `never` — тип для значений, которых никогда не будет

```ts
function throwError(): never {
  throw new Error('Ошибка');
}

function infiniteLoop(): never {
  while (true) {}
}

// В union типах never исчезает
type Example = string | never;  // Результат: string
```

**Используется для:**
- Функций, которые всегда выбрасывают ошибку
- Функций с бесконечными циклами
- Исчерпывающих проверок в switch/case

### Массивы

```ts
let numbers: number[] = [1, 2, 3];
let names: Array<string> = ['Алекс', 'Мария'];  // Альтернативный синтаксис
let mixed: (number | string)[] = [1, 'два', 3];  // Union типы
```

### Кортежи (Tuples)

```ts
let person: [string, number] = ['Алекс', 25];  // [имя, возраст]
let coordinates: [number, number] = [10, 20];  // [x, y]
```

### Объекты

```ts
let user: { name: string; age: number } = {
  name: 'Алекс',
  age: 25
};
```

### Enum (перечисления)

```ts
enum Status {
  Pending,
  Completed,
  Failed
}

let status: Status = Status.Pending;
```

### Примеры использования

```ts
// Примитивные типы
let count: number = 10;
let name: string = 'Алекс';
let isActive: boolean = true;

// Массивы
let numbers: number[] = [1, 2, 3];
let users: string[] = ['Алекс', 'Мария'];

// Объекты
let user: { 
  id: number; 
  name: string; 
  email: string 
} = {
  id: 1,
  name: 'Алекс',
  email: 'alex@example.com'
};

// Функции
function add(a: number, b: number): number {
  return a + b;
}

function greet(name: string): void {
  console.log(`Привет, ${name}!`);
}
```

### Вывод типов (Type Inference)

TypeScript может автоматически определять типы:

```ts
let age = 25;        // TypeScript выводит: number
let name = 'Алекс';  // TypeScript выводит: string
let active = true;   // TypeScript выводит: boolean

// Явная типизация не обязательна, но иногда полезна
let age: number = 25;
```

### Визуальная аналогия

Базовые типы — как **разные виды контейнеров**:
- `number` — контейнер для чисел
- `string` — контейнер для текста
- `boolean` — контейнер для да/нет
- `any` — универсальный контейнер (что угодно, но небезопасно)
- `unknown` — закрытый контейнер (нужно проверить, что внутри)
- `void` — пустой контейнер (ничего)
- `never` — контейнер, который никогда не будет заполнен

### ⚠️ Частая ошибка

Путают `any` и `unknown`:

```ts
// ❌ Плохо
let value: any = getUserData();
value.foo.bar;  // Компилируется, но может быть ошибка в runtime

// ✅ Хорошо
let value: unknown = getUserData();
if (typeof value === 'object' && value !== null && 'foo' in value) {
  // Теперь безопасно
  (value as { foo: { bar: unknown } }).foo.bar;
}
```

### Итоги

- Базовые типы: `number`, `string`, `boolean`, `null`, `undefined`
- Специальные типы: `any`, `unknown`, `void`, `never`
- Массивы: `number[]` или `Array<number>`
- Кортежи: `[string, number]`
- Объекты: `{ name: string; age: number }`
- TypeScript может автоматически выводить типы
- Избегай `any`, используй `unknown` для неизвестных типов

---

## 2. Разница между `any`, `unknown`, `never`

`any`, `unknown` и `never` — это **специальные типы** в TypeScript, которые ведут себя по-разному.

### `any` — любой тип (отключает проверку)

`any` — это способ **отключить проверку типов** для переменной. Можно присвоить что угодно и использовать как угодно.

```ts
let value: any = 42;
value = 'привет';      // ✅ Можно
value = true;          // ✅ Можно
value = {};            // ✅ Можно
value = null;          // ✅ Можно
value = undefined;     // ✅ Можно

// Можно использовать без проверки
value.foo.bar.baz;     // ✅ Компилируется (но может быть ошибка в runtime!)
value();               // ✅ Компилируется (но может быть ошибка в runtime!)
value[0];              // ✅ Компилируется
value.toUpperCase();   // ✅ Компилируется (хотя это число!)
```

**Проблема:** TypeScript **не проверяет** операции с `any`. Ошибки обнаруживаются только в runtime.

### `unknown` — неизвестный тип (безопасная альтернатива)

`unknown` — это **безопасная** альтернатива `any`. Можно присвоить что угодно, но **нельзя использовать** без проверки типа.

```ts
let value: unknown = 42;
value = 'привет';      // ✅ Можно присвоить
value = true;          // ✅ Можно присвоить
value = {};            // ✅ Можно присвоить
value = null;          // ✅ Можно присвоить
value = undefined;     // ✅ Можно присвоить

// НО! Нельзя использовать без проверки
value.foo;             // ❌ Ошибка: Object is of type 'unknown'
value();               // ❌ Ошибка: Object is of type 'unknown'
value[0];              // ❌ Ошибка: Object is of type 'unknown'
value.toUpperCase();   // ❌ Ошибка: Object is of type 'unknown'

// Нужно сначала проверить тип
if (typeof value === 'string') {
  value.toUpperCase();  // ✅ Теперь можно, TypeScript знает, что это string
}

if (typeof value === 'number') {
  value.toFixed(2);     // ✅ Теперь можно
}
```

**Преимущество:** Заставляет проверять тип перед использованием.

### `never` — тип для значений, которых никогда не будет

`never` используется для обозначения значений, которые **никогда не произойдут**. Переменная типа `never` не может иметь значения.

```ts
// Функция, которая всегда выбрасывает ошибку
function throwError(): never {
  throw new Error('Ошибка');
}

// Функция с бесконечным циклом
function infiniteLoop(): never {
  while (true) {
    // Никогда не выйдет отсюда
  }
}

// В switch/case для исчерпывающих проверок
type Status = 'success' | 'error';

function handleStatus(status: Status): string {
  switch (status) {
    case 'success':
      return 'Успех';
    case 'error':
      return 'Ошибка';
    default:
      const _exhaustive: never = status;  // Если добавят новый вариант, будет ошибка
      return _exhaustive;
  }
}
```

**Особенность в union типах:**
```ts
type Example = string | never;  // Результат: string (never исчезает)
type Example2 = string | number | never;  // Результат: string | number
```

### Сравнительная таблица

| Свойство | `any` | `unknown` | `never` |
|----------|-------|-----------|---------|
| **Можно присвоить что угодно?** | ✅ Да | ✅ Да | ❌ Нет (нельзя присвоить ничего) |
| **Можно использовать без проверки?** | ✅ Да | ❌ Нет | ❌ Нет (невозможно использовать) |
| **Проверка типов** | ❌ Отключена | ✅ Включена (но нужно проверить) | ✅ Включена |
| **Безопасность** | ❌ Небезопасно | ✅ Безопасно | ✅ Безопасно |
| **Когда использовать** | ⚠️ Почти никогда | ✅ Для неизвестных данных | ✅ Для невозможных значений |

### Примеры использования

#### `any` — избегай (но иногда нужно для миграции)

```ts
// ❌ Плохо — теряем безопасность типов
function processData(data: any) {
  return data.value.toUpperCase();  // Может упасть в runtime
}

// ✅ Лучше использовать unknown
function processData(data: unknown) {
  if (typeof data === 'object' && data !== null && 'value' in data) {
    const value = (data as { value: unknown }).value;
    if (typeof value === 'string') {
      return value.toUpperCase();
    }
  }
  throw new Error('Неверный формат данных');
}
```

#### `unknown` — для динамических данных

```ts
// Данные из API (не знаем точную структуру)
async function fetchUser(id: number): Promise<unknown> {
  const response = await fetch(`/api/users/${id}`);
  return response.json();
}

// Использование
const user = await fetchUser(1);

// Нужно проверить структуру
if (
  typeof user === 'object' &&
  user !== null &&
  'name' in user &&
  typeof (user as { name: unknown }).name === 'string'
) {
  console.log((user as { name: string }).name);
}
```

#### `never` — для исчерпывающих проверок

```ts
type Theme = 'light' | 'dark';

function getThemeColor(theme: Theme): string {
  switch (theme) {
    case 'light':
      return '#ffffff';
    case 'dark':
      return '#000000';
    default:
      // Если добавят новый вариант Theme, TypeScript покажет ошибку здесь
      const exhaustiveCheck: never = theme;
      return exhaustiveCheck;
  }
}

// Если добавят 'auto' в Theme:
// type Theme = 'light' | 'dark' | 'auto';
// Ошибка: Type 'string' is not assignable to type 'never'
```

### Визуальная аналогия

- **`any`** — как **коробка с надписью "что угодно"** — можно открыть и достать что угодно без проверки (опасно!)
- **`unknown`** — как **закрытая коробка** — можешь положить что угодно, но перед использованием нужно открыть и проверить содержимое (безопасно)
- **`never`** — как **пустая коробка, которой никогда не будет** — она не существует, используется для обозначения невозможных ситуаций

### ⚠️ Частая ошибка

Используют `any` вместо `unknown`:

```ts
// ❌ Плохо
function processData(data: any) {
  return data.value.toUpperCase();  // Небезопасно
}

// ✅ Хорошо
function processData(data: unknown) {
  if (typeof data === 'object' && data !== null && 'value' in data) {
    const value = (data as { value: unknown }).value;
    if (typeof value === 'string') {
      return value.toUpperCase();
    }
  }
  throw new Error('Неверный формат');
}
```

### Итоги

- `any` — отключает проверку типов (избегай!)
- `unknown` — безопасная альтернатива `any`, требует проверки типа перед использованием
- `never` — тип для невозможных значений, используется для исчерпывающих проверок
- Используй `unknown` вместо `any` для неизвестных данных
- Используй `never` для функций, которые никогда не возвращают значение, и для исчерпывающих проверок

---

## 3. Почему `any` — плохо

`any` — это тип, который **отключает проверку типов** в TypeScript. Хотя это кажется удобным, это **антипаттерн**, который лишает тебя преимуществ TypeScript.

### Проблемы с `any`

#### 1. Потеря проверки типов

**С `any`:**
```ts
let value: any = 42;

value.foo.bar.baz;     // ✅ Компилируется
value();               // ✅ Компилируется
value.toUpperCase();   // ✅ Компилируется (хотя это число!)
value[0].name;         // ✅ Компилируется

// Все эти операции могут упасть в runtime!
```

**Без `any`:**
```ts
let value: number = 42;

value.foo.bar.baz;     // ❌ Property 'foo' does not exist on type 'number'
value();               // ❌ This expression is not callable
value.toUpperCase();   // ❌ Property 'toUpperCase' does not exist on type 'number'
value[0].name;         // ❌ Type 'number' is not indexable
```

**Результат:** TypeScript не может помочь найти ошибки.

#### 2. Потеря автодополнения

```ts
interface User {
  id: number;
  name: string;
  email: string;
  age: number;
}

// С any — нет автодополнения
function processUser(user: any) {
  user.  // IDE не показывает: id, name, email, age
  user.na  // Не предлагает: name
}

// Без any — есть автодополнение
function processUser(user: User) {
  user.  // IDE показывает: id, name, email, age
  user.na  // Предлагает: name
}
```

#### 3. Ошибки обнаруживаются только в runtime

```ts
function calculateTotal(items: any[]): number {
  return items.reduce((sum, item) => {
    return sum + item.price;  // Может быть ошибка, если price не существует
  }, 0);
}

// Компилируется без ошибок
calculateTotal([{ name: 'Product' }]);  // ❌ Runtime ошибка: Cannot read property 'price' of undefined
```

**С правильной типизацией:**
```ts
interface Item {
  price: number;
}

function calculateTotal(items: Item[]): number {
  return items.reduce((sum, item) => {
    return sum + item.price;  // ✅ TypeScript знает, что price существует
  }, 0);
}

// Ошибка компиляции
calculateTotal([{ name: 'Product' }]);  
// ❌ Type '{ name: string }' is not assignable to type 'Item'
```

#### 4. "Заражение" типов

`any` распространяется на другие части кода:

```ts
function getData(): any {
  return { name: 'Алекс', age: 25 };
}

const user = getData();  // user: any

// Теперь все операции с user не проверяются
user.name.toUpperCase();  // ✅ Компилируется (но может быть ошибка)
user.age.toFixed(2);      // ✅ Компилируется (но может быть ошибка)
user.email.toLowerCase(); // ✅ Компилируется (но email может не существовать!)
```

**С правильной типизацией:**
```ts
interface User {
  name: string;
  age: number;
}

function getData(): User {
  return { name: 'Алекс', age: 25 };
}

const user = getData();  // user: User

user.name.toUpperCase();  // ✅ Компилируется, TypeScript знает, что name - string
user.age.toFixed(2);      // ✅ Компилируется, TypeScript знает, что age - number
user.email.toLowerCase(); // ❌ Property 'email' does not exist on type 'User'
```

#### 5. Сложность рефакторинга

```ts
// Представим, что мы меняем структуру данных
interface OldUser {
  firstName: string;
  lastName: string;
}

interface NewUser {
  fullName: string;  // Изменили структуру
}

// С any — TypeScript не поможет найти все места
function processUser(user: any) {
  return user.firstName + ' ' + user.lastName;  // Старый код, но компилируется
}

// Без any — TypeScript покажет все ошибки
function processUser(user: NewUser) {
  return user.firstName + ' ' + user.lastName;  
  // ❌ Property 'firstName' does not exist on type 'NewUser'
  // Нужно изменить на: user.fullName
}
```

### Примеры проблем

#### Пример 1: Ошибка в runtime

```ts
// ❌ Плохо
function formatPrice(price: any): string {
  return price.toFixed(2) + ' ₽';
}

formatPrice(42);      // ✅ "42.00 ₽"
formatPrice('42');    // ❌ Runtime ошибка: price.toFixed is not a function
formatPrice(null);    // ❌ Runtime ошибка: Cannot read property 'toFixed' of null
```

```ts
// ✅ Хорошо
function formatPrice(price: number): string {
  return price.toFixed(2) + ' ₽';
}

formatPrice(42);      // ✅ "42.00 ₽"
formatPrice('42');    // ❌ Ошибка компиляции: Argument of type 'string' is not assignable to parameter of type 'number'
formatPrice(null);    // ❌ Ошибка компиляции: Argument of type 'null' is not assignable to parameter of type 'number'
```

#### Пример 2: Потеря автодополнения

```ts
// ❌ Плохо
function processData(data: any) {
  data.  // Нет подсказок, что может быть в data
}

// ✅ Хорошо
interface Data {
  id: number;
  name: string;
  value: number;
}

function processData(data: Data) {
  data.  // IDE показывает: id, name, value
}
```

### Когда `any` допустим?

`any` допустим только в **исключительных случаях**:

1. **Миграция с JavaScript на TypeScript** (временно)
2. **Работа с устаревшими библиотеками** без типов
3. **Динамический код**, который невозможно типизировать

**Но даже тогда лучше:**
- Использовать `unknown` вместо `any`
- Типизировать постепенно
- Использовать `@ts-ignore` только для конкретных строк, не функций

### Альтернативы `any`

#### 1. Используй `unknown`

```ts
// ❌ Плохо
function process(data: any) {
  return data.value.toUpperCase();
}

// ✅ Хорошо
function process(data: unknown) {
  if (
    typeof data === 'object' &&
    data !== null &&
    'value' in data &&
    typeof (data as { value: unknown }).value === 'string'
  ) {
    return (data as { value: string }).value.toUpperCase();
  }
  throw new Error('Неверный формат данных');
}
```

#### 2. Типизируй постепенно

```ts
// ❌ Плохо
function fetchData(): any {
  return fetch('/api/data').then(res => res.json());
}

// ✅ Хорошо
interface ApiResponse {
  data: {
    id: number;
    name: string;
  }[];
}

function fetchData(): Promise<ApiResponse> {
  return fetch('/api/data').then(res => res.json());
}
```

#### 3. Используй generics

```ts
// ❌ Плохо
function getValue(obj: any, key: any): any {
  return obj[key];
}

// ✅ Хорошо
function getValue<T, K extends keyof T>(obj: T, key: K): T[K] {
  return obj[key];
}
```

### Визуальная аналогия

`any` — как **отключение всех сигналов светофора**:
- Кажется, что можно ехать быстрее
- Но риск аварий намного выше
- В итоге замедляет движение из-за аварий

**Правильная типизация** — как **соблюдение правил дорожного движения**:
- Может быть медленнее на старте
- Но безопаснее и предсказуемее
- Меньше проблем в долгосрочной перспективе

### ⚠️ Частая ошибка

Думают, что `any` ускоряет разработку:

```ts
// ❌ Неправильное мышление
// "any быстрее, не нужно думать о типах"

// ✅ Реальность
// any замедляет разработку на больших проектах
// Больше времени тратится на отладку runtime ошибок
// Сложнее рефакторить код
```

### Итоги

- `any` отключает проверку типов — теряешь преимущества TypeScript
- Потеря автодополнения и подсказок IDE
- Ошибки обнаруживаются только в runtime, а не при компиляции
- "Заражение" типов — `any` распространяется на другие части кода
- Сложность рефакторинга — TypeScript не может помочь найти проблемы
- Используй `unknown` вместо `any` для неизвестных данных
- Типизируй постепенно — лучше медленно, но правильно
- `any` допустим только в исключительных случаях (миграция, устаревшие библиотеки)

---

## 4. Когда использовать `unknown`

`unknown` — это **безопасная альтернатива `any`**. Используй `unknown`, когда тип данных **неизвестен** и нужно проверить его перед использованием.

### Основное правило

**Используй `unknown`, когда:**
- Получаешь данные из внешнего источника (API, файл, пользовательский ввод)
- Тип данных неизвестен на этапе компиляции
- Нужна безопасная работа с динамическими данными

**Не используй `unknown`, когда:**
- Тип данных известен — используй конкретный тип
- Можно использовать более конкретный union тип

### Пример 1: Данные из API

```ts
// ❌ Плохо — используем any
async function fetchUser(id: number): Promise<any> {
  const response = await fetch(`/api/users/${id}`);
  return response.json();
}

const user = await fetchUser(1);
user.name.toUpperCase();  // ❌ Может упасть, если name нет или не string

// ✅ Хорошо — используем unknown
async function fetchUser(id: number): Promise<unknown> {
  const response = await fetch(`/api/users/${id}`);
  return response.json();
}

const user = await fetchUser(1);

// Проверяем структуру перед использованием
if (
  typeof user === 'object' &&
  user !== null &&
  'name' in user &&
  typeof (user as { name: unknown }).name === 'string'
) {
  const userName = (user as { name: string }).name;
  userName.toUpperCase();  // ✅ Безопасно
}
```

### Пример 2: JSON.parse

```ts
// ❌ Плохо
function parseJson(json: string): any {
  return JSON.parse(json);
}

const data = parseJson('{"name": "Алекс"}');
data.name.toUpperCase();  // ❌ Небезопасно

// ✅ Хорошо
function parseJson(json: string): unknown {
  return JSON.parse(json);
}

const data = parseJson('{"name": "Алекс"}');

// Проверяем структуру
if (
  typeof data === 'object' &&
  data !== null &&
  'name' in data &&
  typeof (data as { name: unknown }).name === 'string'
) {
  const name = (data as { name: string }).name;
  name.toUpperCase();  // ✅ Безопасно
}
```

### Пример 3: Пользовательский ввод

```ts
// Пользователь отправляет данные формы
function processFormData(data: unknown): void {
  // Проверяем, что это объект
  if (typeof data !== 'object' || data === null) {
    throw new Error('Ожидается объект');
  }

  // Проверяем конкретные поля
  if (!('email' in data) || typeof (data as { email: unknown }).email !== 'string') {
    throw new Error('Поле email обязательно и должно быть строкой');
  }

  if (!('age' in data) || typeof (data as { age: unknown }).age !== 'number') {
    throw new Error('Поле age обязательно и должно быть числом');
  }

  // Теперь безопасно использовать
  const email = (data as { email: string; age: number }).email;
  const age = (data as { email: string; age: number }).age;

  console.log(`Email: ${email}, Age: ${age}`);
}
```

### Пример 4: Тип-гаards (проверки типов)

Создай функции-проверки для переиспользования:

```ts
// Проверка, что это объект с полем name
function hasName(obj: unknown): obj is { name: string } {
  return (
    typeof obj === 'object' &&
    obj !== null &&
    'name' in obj &&
    typeof (obj as { name: unknown }).name === 'string'
  );
}

// Использование
function processUser(user: unknown): void {
  if (hasName(user)) {
    // TypeScript знает, что user имеет name: string
    console.log(user.name.toUpperCase());  // ✅ Безопасно
  } else {
    throw new Error('Пользователь должен иметь поле name');
  }
}
```

### Пример 5: Обработка ошибок

```ts
function processError(error: unknown): string {
  // Проверяем тип ошибки
  if (error instanceof Error) {
    return error.message;  // ✅ Безопасно
  }

  if (typeof error === 'string') {
    return error;  // ✅ Безопасно
  }

  if (
    typeof error === 'object' &&
    error !== null &&
    'message' in error &&
    typeof (error as { message: unknown }).message === 'string'
  ) {
    return (error as { message: string }).message;  // ✅ Безопасно
  }

  return 'Неизвестная ошибка';
}

try {
  // что-то может выбросить ошибку
} catch (error) {
  // error: unknown
  const message = processError(error);  // ✅ Безопасно
}
```

### Создание type guards для `unknown`

```ts
// Проверка на объект с определёнными полями
interface User {
  id: number;
  name: string;
  email: string;
}

function isUser(obj: unknown): obj is User {
  return (
    typeof obj === 'object' &&
    obj !== null &&
    'id' in obj &&
    typeof (obj as { id: unknown }).id === 'number' &&
    'name' in obj &&
    typeof (obj as { name: unknown }).name === 'string' &&
    'email' in obj &&
    typeof (obj as { email: unknown }).email === 'string'
  );
}

// Использование
function processUser(data: unknown): void {
  if (isUser(data)) {
    // TypeScript знает, что data: User
    console.log(data.id, data.name, data.email);  // ✅ Безопасно
  }
}
```

### Когда НЕ использовать `unknown`

#### 1. Когда тип известен

```ts
// ❌ Избыточно
function greet(name: unknown): void {
  if (typeof name === 'string') {
    console.log(`Привет, ${name}!`);
  }
}

// ✅ Лучше
function greet(name: string): void {
  console.log(`Привет, ${name}!`);
}
```

#### 2. Когда можно использовать union тип

```ts
// ❌ Можно лучше
function process(value: unknown): void {
  if (typeof value === 'string') {
    // ...
  } else if (typeof value === 'number') {
    // ...
  }
}

// ✅ Лучше
function process(value: string | number): void {
  if (typeof value === 'string') {
    // TypeScript автоматически сузит тип
  } else {
    // TypeScript знает, что это number
  }
}
```

### Сравнение с `any`

| Ситуация | `any` | `unknown` |
|----------|-------|-----------|
| **Данные из API** | ❌ Небезопасно | ✅ Безопасно (нужна проверка) |
| **JSON.parse** | ❌ Небезопасно | ✅ Безопасно (нужна проверка) |
| **Пользовательский ввод** | ❌ Небезопасно | ✅ Безопасно (нужна проверка) |
| **Обработка ошибок** | ❌ Небезопасно | ✅ Безопасно (нужна проверка) |

### Визуальная аналогия

`unknown` — как **закрытая коробка с подарком**:
- Не знаешь, что внутри
- Нужно открыть и проверить содержимое
- Только после проверки можешь использовать

`any` — как **открытая коробка, в которую можно положить что угодно**:
- Можно использовать сразу, без проверки
- Но рискуешь найти не то, что ожидал

### ⚠️ Частая ошибка

Используют `any` вместо `unknown`, потому что проще:

```ts
// ❌ Плохо — проще, но небезопасно
function process(data: any) {
  return data.value.toUpperCase();
}

// ✅ Хорошо — нужно проверить, но безопасно
function process(data: unknown) {
  if (
    typeof data === 'object' &&
    data !== null &&
    'value' in data &&
    typeof (data as { value: unknown }).value === 'string'
  ) {
    return (data as { value: string }).value.toUpperCase();
  }
  throw new Error('Неверный формат данных');
}
```

### Итоги

- Используй `unknown` для данных из внешних источников (API, файлы, пользовательский ввод)
- Используй `unknown` когда тип неизвестен на этапе компиляции
- Всегда проверяй тип перед использованием `unknown`
- Создавай type guards для переиспользования проверок
- Не используй `unknown`, если можно использовать конкретный тип или union тип
- `unknown` безопаснее `any` — заставляет проверять тип перед использованием

---

## 5. Тип `void`

`void` в TypeScript означает **отсутствие возвращаемого значения**. Используется для функций, которые ничего не возвращают.

### Что такое `void`?

`void` — это тип, который означает, что функция **не возвращает значение** (или возвращает `undefined`).

```ts
function logMessage(): void {
  console.log('Привет');
  // Нет return или return без значения
}

function greet(name: string): void {
  console.log(`Привет, ${name}!`);
  return;  // Можно явно указать return, но без значения
}

function process(): void {
  // Функция что-то делает, но не возвращает результат
  const data = fetchData();
  saveData(data);
  // Нет return
}
```

### Когда использовать `void`?

#### 1. Функции, которые ничего не возвращают

```ts
function showAlert(message: string): void {
  alert(message);
}

function saveToStorage(key: string, value: string): void {
  localStorage.setItem(key, value);
}

function logError(error: Error): void {
  console.error('Ошибка:', error.message);
}
```

#### 2. Обработчики событий

```ts
function handleClick(): void {
  console.log('Кнопка нажата');
}

button.addEventListener('click', handleClick);
```

#### 3. Методы, которые изменяют состояние

```ts
class Counter {
  private count: number = 0;

  increment(): void {  // Изменяет состояние, но не возвращает значение
    this.count++;
  }

  reset(): void {
    this.count = 0;
  }

  getValue(): number {  // Возвращает значение
    return this.count;
  }
}
```

### `void` vs отсутствие типа

```ts
// Явное указание void
function log1(): void {
  console.log('Привет');
}

// Без указания типа (TypeScript выводит void)
function log2() {
  console.log('Привет');
}

// Оба варианта эквивалентны
// Но лучше явно указывать void для ясности
```

### `void` в типах функций

```ts
// Тип функции, которая ничего не возвращает
type ClickHandler = () => void;

const handler: ClickHandler = () => {
  console.log('Клик');
};

// Массив функций, которые ничего не возвращают
const handlers: (() => void)[] = [
  () => console.log('Один'),
  () => console.log('Два'),
  () => console.log('Три'),
];
```

### Особенности `void`

#### 1. `void` может быть присвоен только `undefined`

```ts
let value: void = undefined;  // ✅ Разрешено
let value2: void = null;      // ❌ Type 'null' is not assignable to type 'void' (с strictNullChecks)
let value3: void = 42;        // ❌ Type 'number' is not assignable to type 'void'
```

#### 2. Функция с `void` может возвращать значение (но оно игнорируется)

```ts
function example(): void {
  return 42;  // ⚠️ Технически разрешено, но значение игнорируется
}

const result = example();  // result: void (не 42!)
```

**Но лучше не возвращать значение из функции типа `void`** — это может ввести в заблуждение.

#### 3. `void` в arrow functions

```ts
const log = (): void => {
  console.log('Привет');
};

// Или короче
const log = (): void => console.log('Привет');
```

### Разница с `undefined`

```ts
// void — означает отсутствие возвращаемого значения (игнорируется)
function log1(): void {
  console.log('Привет');
  return;  // Можно вернуть undefined явно, но обычно не нужно
}

// undefined — явно возвращает undefined
function log2(): undefined {
  console.log('Привет');
  return undefined;  // Нужно явно вернуть undefined
}

// Обычно используют void, а не undefined
```

### Примеры использования

#### Пример 1: Callbacks

```ts
function forEach<T>(arr: T[], callback: (item: T) => void): void {
  for (const item of arr) {
    callback(item);  // Вызываем callback, игнорируя возвращаемое значение
  }
}

forEach([1, 2, 3], (item) => {
  console.log(item);  // Функция ничего не возвращает (void)
});
```

#### Пример 2: React event handlers

```tsx
// Event handler в React — обычно void
function Button() {
  const handleClick = (): void => {
    console.log('Кнопка нажата');
  };

  return <button onClick={handleClick}>Нажми меня</button>;
}
```

#### Пример 3: Методы класса

```ts
class TodoList {
  private todos: string[] = [];

  add(todo: string): void {  // Добавляет элемент, но не возвращает
    this.todos.push(todo);
  }

  remove(index: number): void {  // Удаляет элемент, но не возвращает
    this.todos.splice(index, 1);
  }

  getAll(): string[] {  // Возвращает массив
    return [...this.todos];
  }
}
```

### Визуальная аналогия

`void` — как **действие без результата**:
- **Выключаешь свет** — действие выполнено, но нет возвращаемого значения
- **Печатаешь документ** — документ напечатан, но функция ничего не возвращает
- **Логируешь сообщение** — сообщение выведено, но функции нечего вернуть

**В отличие от:**
- **Вычисляешь сумму** — возвращается число (не `void`)
- **Получаешь пользователя** — возвращается объект (не `void`)

### ⚠️ Частая ошибка

Путают `void` и `undefined`:

```ts
// ❌ Неправильно думать
// "void и undefined — одно и то же"

// ✅ Правильно
// void — означает "ничего не возвращает" (игнорируется)
// undefined — это конкретное значение, которое можно вернуть
```

### Итоги

- `void` — тип для функций, которые ничего не возвращают
- Используется для функций, которые выполняют действия, но не возвращают результат
- Обработчики событий, методы изменения состояния — типичные случаи использования
- `void` может быть присвоен только `undefined`
- Лучше явно указывать `void` для ясности, хотя TypeScript может вывести его автоматически
- `void` отличается от `undefined` — `void` означает отсутствие возвращаемого значения, `undefined` — конкретное значение

---

## 6. Типы `null` и `undefined`

`null` и `undefined` в TypeScript — это **отдельные типы**, которые представляют отсутствие значения, но по-разному.

### Что такое `null` и `undefined`?

#### `null` — явное отсутствие значения

```ts
let value: null = null;  // Явно указываем, что значения нет

// Обычно используется, когда значение намеренно отсутствует
let user: { name: string } | null = null;  // Пользователь ещё не загружен
```

#### `undefined` — значение не определено

```ts
let value: undefined = undefined;  // Значение не было присвоено

// Обычно используется для опциональных значений
function greet(name?: string): void {
  // name может быть undefined
  console.log(`Привет, ${name || 'незнакомец'}!`);
}
```

### Разница между `null` и `undefined`

| Свойство | `null` | `undefined` |
|----------|--------|-------------|
| **Значение** | Явное отсутствие значения | Значение не определено |
| **Использование** | Намеренное отсутствие | Не было присвоено значение |
| **Оператор `typeof`** | `"object"` (баг в JS) | `"undefined"` |
| **Оператор `==`** | `null == undefined` → `true` | `null == undefined` → `true` |
| **Оператор `===`** | `null === undefined` → `false` | `null === undefined` → `false` |

### Поведение без `strictNullChecks`

**Без `strictNullChecks` (по умолчанию в старых версиях):**

```ts
let name: string;
name = null;        // ✅ Разрешено
name = undefined;   // ✅ Разрешено

function greet(name: string): void {
  console.log(name.toUpperCase());  // Может быть null/undefined!
}

greet(null);  // ❌ Runtime ошибка, но компилируется
```

**Проблема:** Любой тип может быть `null` или `undefined`, что приводит к ошибкам в runtime.

### Поведение с `strictNullChecks`

**С `strictNullChecks: true` (рекомендуется):**

```ts
let name: string;
name = null;        // ❌ Type 'null' is not assignable to type 'string'
name = undefined;   // ❌ Type 'undefined' is not assignable to type 'string'

// Нужно явно указать
let name: string | null = null;      // ✅ Разрешено
let name2: string | undefined = undefined;  // ✅ Разрешено
let name3: string | null | undefined = null;  // ✅ Разрешено

function greet(name: string): void {
  console.log(name.toUpperCase());  // name не может быть null/undefined
}

greet(null);  // ❌ Argument of type 'null' is not assignable to parameter of type 'string'
```

**Преимущество:** TypeScript заставляет обрабатывать случаи с `null` и `undefined`.

### Явное использование `null` и `undefined`

#### `null` — для явного отсутствия значения

```ts
// Пользователь ещё не загружен
let currentUser: { name: string; email: string } | null = null;

async function loadUser() {
  const user = await fetchUser();
  currentUser = user;  // Теперь не null
}

function getUserName(): string {
  if (currentUser === null) {
    return 'Гость';
  }
  return currentUser.name;  // TypeScript знает, что currentUser не null
}
```

#### `undefined` — для опциональных значений

```ts
// Опциональные свойства объекта
interface User {
  name: string;
  email?: string;  // Может быть undefined
}

const user: User = {
  name: 'Алекс'
  // email не указан, значит undefined
};

if (user.email !== undefined) {
  console.log(user.email.toUpperCase());  // ✅ Безопасно
}
```

### Опциональные параметры и свойства

#### Опциональные параметры функций

```ts
// Параметр может быть undefined
function greet(name?: string): void {
  // name: string | undefined
  if (name === undefined) {
    console.log('Привет, незнакомец!');
  } else {
    console.log(`Привет, ${name}!`);
  }
}

greet();           // name = undefined
greet('Алекс');    // name = 'Алекс'
```

#### Опциональные свойства объектов

```ts
interface User {
  name: string;
  email?: string;  // Опциональное свойство (может быть undefined)
  age?: number;
}

const user1: User = {
  name: 'Алекс'
  // email и age не указаны, значит undefined
};

const user2: User = {
  name: 'Алекс',
  email: 'alex@example.com'
  // age не указан, значит undefined
};
```

### Проверка на `null` и `undefined`

#### Type narrowing (сужение типа)

```ts
function processValue(value: string | null | undefined): string {
  // Проверка на null
  if (value === null) {
    return 'Значение отсутствует';
  }

  // Проверка на undefined
  if (value === undefined) {
    return 'Значение не определено';
  }

  // TypeScript знает, что value: string здесь
  return value.toUpperCase();  // ✅ Безопасно
}
```

#### Оператор `??` (nullish coalescing)

```ts
function greet(name: string | null | undefined): string {
  // Возвращает правую часть, если левая null или undefined
  return `Привет, ${name ?? 'незнакомец'}!`;
}

greet('Алекс');    // "Привет, Алекс!"
greet(null);       // "Привет, незнакомец!"
greet(undefined);  // "Привет, незнакомец!"
```

#### Опциональная цепочка (`?.`)

```ts
interface User {
  name: string;
  address?: {
    city: string;
    street?: string;
  };
}

function getCity(user: User | null | undefined): string | undefined {
  // Безопасный доступ к вложенным свойствам
  return user?.address?.city;
}

const user: User | null = null;
getCity(user);  // undefined (не ошибка!)
```

### Примеры использования

#### Пример 1: Загрузка данных

```ts
interface User {
  id: number;
  name: string;
}

// null означает, что данные ещё не загружены
let user: User | null = null;

async function loadUser() {
  user = await fetchUser();  // Теперь user не null
}

function getUserName(): string {
  if (user === null) {
    return 'Загрузка...';
  }
  return user.name;
}
```

#### Пример 2: Опциональные поля формы

```ts
interface FormData {
  name: string;
  email: string;
  phone?: string;  // Опциональное поле (может быть undefined)
}

function submitForm(data: FormData): void {
  console.log(`Имя: ${data.name}`);
  console.log(`Email: ${data.email}`);
  
  if (data.phone !== undefined) {
    console.log(`Телефон: ${data.phone}`);
  }
}
```

#### Пример 3: Результат поиска

```ts
function findUser(id: number): User | null {
  // Возвращаем null, если пользователь не найден
  const user = database.find(u => u.id === id);
  return user ?? null;
}

const user = findUser(1);
if (user !== null) {
  console.log(user.name);  // ✅ Безопасно
} else {
  console.log('Пользователь не найден');
}
```

### Визуальная аналогия

- **`null`** — как **пустая коробка с надписью "ничего"** — ты намеренно положил туда ничего
- **`undefined`** — как **коробка, которую ещё не открыли** — неизвестно, что внутри (или ничего)

**Пример:**
- `user = null` — "пользователя нет" (явно указали)
- `user = undefined` — "пользователь не определён" (ещё не знаем)

### ⚠️ Частая ошибка

Используют `null` и `undefined` без проверки:

```ts
// ❌ Плохо — без strictNullChecks
function process(user: { name: string }) {
  return user.name.toUpperCase();
}

process(null);  // ❌ Runtime ошибка, но компилируется

// ✅ Хорошо — с strictNullChecks
function process(user: { name: string } | null) {
  if (user === null) {
    return 'Пользователь не найден';
  }
  return user.name.toUpperCase();  // ✅ Безопасно
}
```

### Итоги

- `null` — явное отсутствие значения
- `undefined` — значение не определено
- Без `strictNullChecks` любой тип может быть `null` или `undefined` (небезопасно)
- С `strictNullChecks` нужно явно указывать `null` и `undefined` в типах (безопасно)
- Используй `null` для явного отсутствия значения
- Используй `undefined` для опциональных значений
- Проверяй `null` и `undefined` перед использованием
- Используй операторы `??` и `?.` для безопасной работы

---

## 7. Что меняет `strictNullChecks`

`strictNullChecks` — это опция компилятора TypeScript, которая **строго проверяет** использование `null` и `undefined`. Когда она включена, TypeScript требует явного указания этих типов.

### Без `strictNullChecks` (по умолчанию в старых версиях)

**Поведение:**

```ts
// Любой тип может быть null или undefined
let name: string;
name = null;        // ✅ Разрешено
name = undefined;   // ✅ Разрешено

function greet(name: string): void {
  console.log(name.toUpperCase());  // ⚠️ name может быть null/undefined!
}

greet(null);        // ❌ Runtime ошибка, но компилируется
greet(undefined);   // ❌ Runtime ошибка, но компилируется
greet('Алекс');     // ✅ Работает
```

**Проблема:** TypeScript не защищает от ошибок с `null` и `undefined`.

### С `strictNullChecks: true` (рекомендуется)

**Поведение:**

```ts
// string не может быть null или undefined
let name: string;
name = null;        // ❌ Type 'null' is not assignable to type 'string'
name = undefined;   // ❌ Type 'undefined' is not assignable to type 'string'
name = 'Алекс';     // ✅ Разрешено

function greet(name: string): void {
  console.log(name.toUpperCase());  // ✅ name точно string, не null/undefined
}

greet(null);        // ❌ Argument of type 'null' is not assignable to parameter of type 'string'
greet(undefined);   // ❌ Argument of type 'undefined' is not assignable to parameter of type 'string'
greet('Алекс');     // ✅ Работает
```

**Преимущество:** TypeScript заставляет явно обрабатывать `null` и `undefined`.

### Что меняется?

#### 1. Нужно явно указывать `null` и `undefined` в типах

**Без `strictNullChecks`:**
```ts
let user: { name: string } = null;  // ✅ Разрешено
```

**С `strictNullChecks`:**
```ts
let user: { name: string } = null;  // ❌ Type 'null' is not assignable to type '{ name: string }'
let user: { name: string } | null = null;  // ✅ Нужно явно указать
```

#### 2. Опциональные свойства используют `undefined`

**Без `strictNullChecks`:**
```ts
interface User {
  name: string;
  email?: string;
}

const user: User = { name: 'Алекс' };
// user.email может быть undefined, но TypeScript не всегда это учитывает
```

**С `strictNullChecks`:**
```ts
interface User {
  name: string;
  email?: string;  // email: string | undefined
}

const user: User = { name: 'Алекс' };
// user.email: string | undefined
if (user.email !== undefined) {
  user.email.toUpperCase();  // ✅ Безопасно
}
```

#### 3. Параметры функций должны обрабатывать `null`/`undefined`

**Без `strictNullChecks`:**
```ts
function process(value: string): void {
  console.log(value.toUpperCase());  // Может упасть, если value null/undefined
}

process(null);  // ❌ Runtime ошибка, но компилируется
```

**С `strictNullChecks`:**
```ts
function process(value: string): void {
  console.log(value.toUpperCase());  // ✅ value точно string
}

process(null);  // ❌ Ошибка компиляции

// Нужно явно обработать
function process(value: string | null): void {
  if (value === null) {
    return;
  }
  console.log(value.toUpperCase());  // ✅ Безопасно
}
```

### Примеры изменений

#### Пример 1: Массивы и методы

**Без `strictNullChecks`:**
```ts
function findUser(users: User[], id: number): User {
  return users.find(u => u.id === id);  // Может вернуть undefined!
}

const user = findUser(users, 1);
console.log(user.name);  // ❌ Может упасть, если user undefined
```

**С `strictNullChecks`:**
```ts
function findUser(users: User[], id: number): User | undefined {
  return users.find(u => u.id === id);  // ✅ Явно указан undefined
}

const user = findUser(users, 1);
if (user !== undefined) {
  console.log(user.name);  // ✅ Безопасно
}
```

#### Пример 2: Опциональные свойства

**Без `strictNullChecks`:**
```ts
interface Config {
  apiUrl: string;
  timeout?: number;
}

function makeRequest(config: Config): void {
  // timeout может быть undefined, но TypeScript может не предупредить
  setTimeout(() => {
    // ...
  }, config.timeout);  // ❌ Может быть undefined
}
```

**С `strictNullChecks`:**
```ts
interface Config {
  apiUrl: string;
  timeout?: number;  // timeout: number | undefined
}

function makeRequest(config: Config): void {
  const timeout = config.timeout ?? 5000;  // ✅ Обрабатываем undefined
  setTimeout(() => {
    // ...
  }, timeout);
}
```

#### Пример 3: Инициализация переменных

**Без `strictNullChecks`:**
```ts
let name: string;
// name не инициализирована, но можно использовать
console.log(name.toUpperCase());  // ❌ Runtime ошибка, но компилируется
```

**С `strictNullChecks`:**
```ts
let name: string;
// name не инициализирована
console.log(name.toUpperCase());  
// ❌ Variable 'name' is used before being assigned

// Нужно инициализировать
let name: string = '';
// Или
let name: string | undefined;
if (name !== undefined) {
  console.log(name.toUpperCase());
}
```

### Как включить `strictNullChecks`?

#### В `tsconfig.json`

```json
{
  "compilerOptions": {
    "strictNullChecks": true
  }
}
```

#### Или через `strict`

```json
{
  "compilerOptions": {
    "strict": true  // Включает strictNullChecks и другие строгие проверки
  }
}
```

**Рекомендация:** Всегда используй `strict: true`, который включает `strictNullChecks` и другие важные проверки.

### Миграция на `strictNullChecks`

Если проект большой и не использует `strictNullChecks`:

1. **Включи постепенно** для новых файлов
2. **Исправляй ошибки** по мере их появления
3. **Используй `!` оператор** (non-null assertion) осторожно и временно
4. **Добавляй проверки** на `null`/`undefined` там, где нужно

```ts
// Временное решение при миграции
function process(value: string | null) {
  const safeValue = value!;  // ⚠️ Non-null assertion (используй осторожно)
  console.log(safeValue.toUpperCase());
}

// Лучше добавить проверку
function process(value: string | null) {
  if (value === null) {
    return;
  }
  console.log(value.toUpperCase());  // ✅ Безопасно
}
```

### Визуальная аналогия

**Без `strictNullChecks`** — как **дом без сигнализации**:
- Можешь забыть закрыть дверь (`null`/`undefined`)
- Никто не предупредит об опасности
- Проблемы обнаруживаются только при ограблении (runtime ошибки)

**С `strictNullChecks`** — как **дом с сигнализацией**:
- Сигнализация требует закрыть все двери
- Предупреждает о потенциальных проблемах
- Помогает избежать ошибок до их возникновения

### ⚠️ Частая ошибка

Отключают `strictNullChecks` из-за ошибок:

```json
// ❌ Плохо
{
  "compilerOptions": {
    "strictNullChecks": false  // Отключили из-за ошибок
  }
}
```

**Правильный подход:**
```json
// ✅ Хорошо
{
  "compilerOptions": {
    "strictNullChecks": true  // Оставляем включенным
  }
}
// И исправляем ошибки, добавляя проверки на null/undefined
```

### Итоги

- `strictNullChecks` строго проверяет использование `null` и `undefined`
- Без `strictNullChecks` любой тип может быть `null`/`undefined` (небезопасно)
- С `strictNullChecks` нужно явно указывать `null`/`undefined` в типах (безопасно)
- Заставляет обрабатывать случаи с `null`/`undefined` перед использованием
- Обнаруживает больше ошибок на этапе компиляции
- Всегда включай `strictNullChecks` (или `strict: true`) для новых проектов
- При миграции исправляй ошибки постепенно, не отключая проверку

---